<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java IO框架笔记</title>
    <url>/article/12425.html</url>
    <content><![CDATA[<h3 id="体系结构：装饰器模式与流的设计哲学"><a href="#体系结构：装饰器模式与流的设计哲学" class="headerlink" title="体系结构：装饰器模式与流的设计哲学"></a>体系结构：装饰器模式与流的设计哲学</h3><h4 id="字节流与字符流的分工"><a href="#字节流与字符流的分工" class="headerlink" title="字节流与字符流的分工"></a>字节流与字符流的分工</h4><p>Java IO 框架包含两套独立的体系：<code>InputStream</code> &#x2F; <code>OutputStream</code>（字节流）和 <code>Reader</code> &#x2F; <code>Writer</code>（字符流）。字节流处理二进制数据（如图片、音频），字符流专门处理文本数据。</p>
<p>两套体系之间的关系：字符流是对字节流的封装，内部通过 <strong><code>InputStreamReader</code></strong> 和 <strong><code>OutputStreamWriter</code></strong> 完成字节到字符的转换，而这个转换过程正是<strong>编码处理</strong>的核心。当文件编码与系统默认编码不一致时，必须显式使用这两个类指定字符集，否则就会出现乱码。</p>
<p>这里先给两张思维导图,认识下java IO的各个类的关系:<br><img src="/uploads/IO%E6%A1%86%E6%9E%B6%E5%A4%A7%E7%9A%84%E4%BD%93%E7%B3%BB.png" alt="IO框架大的体系图"><br><img src="/uploads/IO%E6%A1%86%E6%9E%B6%E5%B0%8F%E7%9A%84%E4%BD%93%E7%B3%BB.png" alt="IO框架小的体系图"></p>
<h4 id="装饰器模式：动态组合功能的艺术"><a href="#装饰器模式：动态组合功能的艺术" class="headerlink" title="装饰器模式：动态组合功能的艺术"></a>装饰器模式：动态组合功能的艺术</h4><p>观察 Java IO 的类图可以看到，<code>FilterInputStream</code> 派生出 <code>BufferedInputStream</code>、<code>DataInputStream</code> 等，它们都“包裹”着一个原始的 <code>InputStream</code>（如 <code>FileInputStream</code>）。这种结构正是<strong>装饰器模式</strong>的经典应用。<br>比方说dataInputStream(bufferedInputStream) 包裹层的这种组合体现了两种流的不同职责：</p>
<ul>
<li><strong><code>BufferedInputStream</code></strong>：负责<strong>传输效率</strong>，通过缓冲减少系统调用。</li>
<li><strong><code>DataInputStream</code></strong>：负责<strong>数据解析</strong>，将字节解析为 <code>int</code>、<code>long</code>、UTF 字符串等 Java 基本类型。</li>
</ul>
<p><strong>优势</strong>：可以在运行时动态地为流叠加功能（如缓冲、数据解析），而无需为每一种功能组合创建大量子类。例如，<code>new DataInputStream(new BufferedInputStream(new FileInputStream(&quot;data.bin&quot;)))</code> 既获得了缓冲的性能提升，又能直接读取 Java 基本类型。</p>
<hr>
<h3 id="性能与机制：缓冲流、readLine-隐患"><a href="#性能与机制：缓冲流、readLine-隐患" class="headerlink" title="性能与机制：缓冲流、readLine 隐患"></a>性能与机制：缓冲流、readLine 隐患</h3><h4 id="缓冲流为什么快"><a href="#缓冲流为什么快" class="headerlink" title="缓冲流为什么快"></a>缓冲流为什么快</h4><p>没有缓冲的流（如 <code>FileInputStream</code>）每次调用 <code>read()</code> 都会直接触发操作系统的一次 <strong>系统调用</strong>（用户态切换到内核态），开销极大。而 <code>BufferedInputStream</code> 内部维护了一个字节数组作为缓冲区，当调用 <code>read()</code> 时，若缓冲区中有数据则直接返回；否则触发一次系统调用，将磁盘数据批量读入缓冲区，然后再返回第一个字节。</p>
<table>
<thead>
<tr>
<th>场景特征</th>
<th>推荐方案</th>
</tr>
</thead>
<tbody><tr>
<td>处理<strong>文本文件</strong>，需要按行读取</td>
<td><code>BufferedReader</code> + <code>InputStreamReader</code> (更推荐) 或 <code>InputStreamReader</code> + <code>BufferedInputStream</code></td>
</tr>
<tr>
<td>处理<strong>二进制文件</strong>或<strong>自定义格式</strong></td>
<td><code>BufferedInputStream</code></td>
</tr>
<tr>
<td>需要直接读取 Java 基本类型（int, long 等）</td>
<td><code>DataInputStream</code> + <code>BufferedInputStream</code></td>
</tr>
<tr>
<td>追求极简且无需按行解析</td>
<td><code>Files.readAllBytes()</code>（小文件）或 <code>BufferedInputStream</code>（大文件）</td>
</tr>
</tbody></table>
<p><strong>性能提升的本质</strong>：用空间换时间，将多次小批量 I&#x2F;O 合并为一次大批量 I&#x2F;O，大幅减少上下文切换次数。</p>
<h4 id="BufferedReader的read-与-readLine-的区别及隐患"><a href="#BufferedReader的read-与-readLine-的区别及隐患" class="headerlink" title="BufferedReader的read() 与 readLine() 的区别及隐患"></a>BufferedReader的read() 与 readLine() 的区别及隐患</h4><p><code>read()</code> 返回 <code>-1</code> 表示结束，<code>readLine()</code> 返回 <code>null</code> 表示结束。</p>
<ul>
<li><strong><code>readLine()</code> 的隐患</strong>：<code>BufferedReader.readLine()</code> 内部会逐字节判断换行符，并动态构建 <code>String</code>。如果一行数据非常大（例如几 MB），就会创建一个大对象，频繁触发 Young GC，甚至导致内存溢出。因此，在处理超大行数据时，建议使用 <code>Scanner</code> 或手动解析 <code>char</code> 数组。</li>
<li><strong><code>read(byte[] b)</code> 的效率优势</strong>：批量读取数据到自定义缓冲区，比逐字节 <code>read()</code> 或逐行 <code>readLine()</code> 更高效且可控。</li>
</ul>
<hr>
<h3 id="底层原理：编码转换、IO-模型的演进、零拷贝"><a href="#底层原理：编码转换、IO-模型的演进、零拷贝" class="headerlink" title="底层原理：编码转换、IO 模型的演进、零拷贝"></a>底层原理：编码转换、IO 模型的演进、零拷贝</h3><h4 id="编码转换的具体过程"><a href="#编码转换的具体过程" class="headerlink" title="编码转换的具体过程"></a>编码转换的具体过程</h4><ul>
<li>JVM 内部统一使用 <strong>UTF-16</strong>（即 Unicode）存储字符串。</li>
<li>当从文件读取字节时，<code>InputStreamReader</code> 根据指定的字符集（如 UTF-8）将字节解码为 Unicode 字符，存入内存。</li>
<li>当写入文件时，<code>OutputStreamWriter</code> 再将 Unicode 字符按目标字符集编码为字节。</li>
<li>除了少部分生僻字占两个char外，其余的中英文都是占用一个char，也就是2个字节&#x3D;16位。需要<strong>避免逐 <code>char</code> 打印</strong>，使用行读取或字符数组批量读取</li>
</ul>
<p>常见的场景：<br>如果文件是 UTF-8 编码，但系统默认编码是 GBK，直接使用 <code>FileReader</code>（默认使用系统编码）会乱码，原因是<code>FileReader</code> 内部使用系统默认编码，此时会将 UTF-8 字节按 GBK 规则解码，导致乱码。必须显式使用 <code>InputStreamReader</code> 并指定 <code>StandardCharsets.UTF_8</code>。</p>
<p>代码示例：<br>错误读取代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">WrongFileReader</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 假设系统默认编码为 GBK，文件实际编码为 UTF-8</span></span><br><span class="line">        <span class="keyword">try</span> (<span class="type">FileReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileReader</span>(<span class="string">&quot;F:\\\\desktop\\utf8.txt&quot;</span>)) &#123;</span><br><span class="line">            <span class="type">int</span> ch;</span><br><span class="line">            <span class="keyword">while</span> ((ch = reader.read()) != -<span class="number">1</span>) &#123;</span><br><span class="line">                System.out.print((<span class="type">char</span>) ch);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>正确读取代码1：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"><span class="keyword">import</span> java.nio.charset.StandardCharsets;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CorrectFileReader</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 明确指定 UTF-8 编码</span></span><br><span class="line">        <span class="keyword">try</span> (<span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">InputStreamReader</span>(  </span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">FileInputStream</span>(<span class="string">&quot;F:\\\\desktop\\utf8.txt&quot;</span>),  </span><br><span class="line">        StandardCharsets.UTF_8))) &#123;</span><br><span class="line">            <span class="comment">// 不要逐char打印，以免遇到生僻字</span></span><br><span class="line">            <span class="comment">/**</span></span><br><span class="line"><span class="comment">            int ch;</span></span><br><span class="line"><span class="comment">            while ((ch = reader.read()) != -1) &#123;</span></span><br><span class="line"><span class="comment">                System.out.print((char) ch);</span></span><br><span class="line"><span class="comment">            &#125;</span></span><br><span class="line"><span class="comment">            **/</span></span><br><span class="line">            <span class="type">char</span>[] buf = <span class="keyword">new</span> <span class="title class_">char</span>[<span class="number">8192</span>];  </span><br><span class="line">            <span class="type">int</span> len;  </span><br><span class="line">            <span class="keyword">while</span> ((len = reader.read(buf)) != -<span class="number">1</span>) &#123;  </span><br><span class="line">                System.out.print(<span class="keyword">new</span> <span class="title class_">String</span>(buf, <span class="number">0</span>, len));  </span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>正确读取代码2：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.*;</span><br><span class="line"><span class="keyword">import</span> java.nio.charset.StandardCharsets;</span><br><span class="line"><span class="keyword">import</span> java.nio.file.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ModernFileReader</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Path</span> <span class="variable">path</span> <span class="operator">=</span> Paths.get(<span class="string">&quot;F:\\\\desktop\\utf8.txt&quot;</span>);</span><br><span class="line">        <span class="keyword">try</span> (<span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> Files.newBufferedReader(path, StandardCharsets.UTF_8)) &#123;</span><br><span class="line">            String line;</span><br><span class="line">            <span class="keyword">while</span> ((line = reader.readLine()) != <span class="literal">null</span>) &#123;</span><br><span class="line">                System.out.println(line);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h4 id="从-BIO-到-NIO：IO-模型的演进"><a href="#从-BIO-到-NIO：IO-模型的演进" class="headerlink" title="从 BIO 到 NIO：IO 模型的演进"></a>从 BIO 到 NIO：IO 模型的演进</h4><p>传统的 <code>FileInputStream</code> 属于 <strong>BIO（同步阻塞 I&#x2F;O）</strong>，每个连接对应一个线程，在高并发网络场景下会引发严重的线程资源消耗。Java 1.4 引入了 <strong>NIO（New I&#x2F;O）</strong>，核心组件是 <code>Channel</code>、<code>Buffer</code> 和 <code>Selector</code>（多路复用器）。</p>
<ul>
<li><strong>BIO 的问题</strong>：线程与连接数线性增长，上下文切换开销大，难以应对 C10K 问题。</li>
<li><strong>NIO 的优势</strong>：一个线程通过 <code>Selector</code> 管理多个 <code>Channel</code>，只有当某个 Channel 有数据可读&#x2F;可写时才进行处理，实现了 <strong>I&#x2F;O 多路复用</strong>，极大地提升了并发能力。</li>
</ul>
<h4 id="零拷贝（Zero-Copy）技术"><a href="#零拷贝（Zero-Copy）技术" class="headerlink" title="零拷贝（Zero-Copy）技术"></a>零拷贝（Zero-Copy）技术</h4><p>在文件传输场景（如 Web 服务器发送静态文件）中经常用到零拷贝技术，有两个使用案例：</p>
<ol>
<li>在spring boot中用FileSystemResource返回文件内容，则Spring 底层会自动利用容器的零拷贝机制（如 Tomcat 的 <code>sendfile</code> 支持）</li>
<li>NIO 的 <code>FileChannel.transferTo()</code> 方法正是利用操作系统的 <strong>零拷贝</strong> 特性（如 Linux 的 <code>sendfile</code> 系统调用），数据直接在<strong>内核空间</strong>完成传输，避免了用户态与内核态之间的 CPU 拷贝，显著降低 CPU 占用并提升吞吐量。</li>
</ol>
<p>以下为传统io跟零拷贝io的对比流程图：</p>
<pre><code class="highlight mermaid">sequenceDiagram
    participant App as 应用程序&lt;br/&gt;(用户空间)
    participant Kernel as 内核空间
    participant Disk as 磁盘

    Note over App,Disk: 传统 I/O 方式 (read + write)
    App-&gt;&gt;Kernel: read() 系统调用
    Kernel-&gt;&gt;Disk: DMA 拷贝 (磁盘→内核缓冲区)
    Disk--&gt;&gt;Kernel: 数据
    Kernel-&gt;&gt;Kernel: CPU 拷贝 (内核缓冲区→用户缓冲区)
    Kernel--&gt;&gt;App: 返回，数据在用户缓冲区
    App-&gt;&gt;Kernel: write() 系统调用
    Kernel-&gt;&gt;Kernel: CPU 拷贝 (用户缓冲区→socket 缓冲区)
    Kernel-&gt;&gt;Disk: DMA 拷贝 (socket 缓冲区→网卡)
    Disk--&gt;&gt;Kernel: 完成
    Kernel--&gt;&gt;App: 返回
    Note right of App: 4 次拷贝 (2 次 DMA + 2 次 CPU)&lt;br/&gt;4 次上下文切换

    Note over App,Disk: 零拷贝方式 (sendfile)
    App-&gt;&gt;Kernel: sendfile() 系统调用
    Kernel-&gt;&gt;Disk: DMA 拷贝 (磁盘→内核缓冲区)
    Disk--&gt;&gt;Kernel: 数据
    Kernel-&gt;&gt;Kernel: CPU 拷贝 (内核缓冲区→socket 缓冲区)&lt;br/&gt;(或使用 scatter-gather 避免)
    Kernel-&gt;&gt;Disk: DMA 拷贝 (socket 缓冲区→网卡)
    Disk--&gt;&gt;Kernel: 完成
    Kernel--&gt;&gt;App: 返回
    Note right of App: 2~3 次拷贝 (2 次 DMA + 0~1 次 CPU)&lt;br/&gt;2 次上下文切换</code></pre>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>JavaIO框架</tag>
      </tags>
  </entry>
  <entry>
    <title>ClassLoader详解</title>
    <url>/article/2135.html</url>
    <content><![CDATA[<h2 id="了解ClassLoader的好处"><a href="#了解ClassLoader的好处" class="headerlink" title="了解ClassLoader的好处"></a>了解ClassLoader的好处</h2><p>有助于了解java类的加载机制,了解web应用服务器是怎么加载进war包并怎么工作的</p>
<h2 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h2><ol>
<li>相信很多人都会在各种文章中见到<code>AppClassloader</code>和<code>SystemClassloader</code>,其实这两个是指同一个东西,只是叫法不同.</li>
<li><code>ClassLoader.getSystemResourceAsStream(name);</code>类似这种,ClassLoader有且仅有提供了几个方法跟systemloader有关的方法.</li>
<li>Bootstrap是爷爷、ExtClassLoader是爸爸、AppClassLoader是孩子（孙子）。但由于爸爸ExtClassLoader太忙了，所以照顾孩子AppClassLoader的责任就有爷爷Bootstrap来处理了(意思是，虽然ExtClassLoader是AppClassLoader老爸，但却是由爷爷Bootstrap加载的AppClassLoader。该情况与中国国情完全一样)。</li>
<li>项目的配置文件的覆盖顺序为：根类加载器 &gt; 扩展类加载器 &gt; 应用中的资源文件 &gt; 应用所依赖的第三方jar</li>
</ol>
<h2 id="各个ClassLoader的关联"><a href="#各个ClassLoader的关联" class="headerlink" title="各个ClassLoader的关联"></a>各个ClassLoader的关联</h2><p>普通java项目:<br><code>AppClassLoader</code>&gt;&gt;<code>ExtClassLoader</code>&gt;&gt;<code>bootstrapLoader</code><br>glassfish web项目:<br><code>WebappClassLoader</code>&gt;&gt;<code>DelegatingClassLoader</code>&gt;&gt;<code>URLClassLoader</code>&gt;&gt;<code>APIClassLoader</code>&gt;&gt;<code>ExtClassLoader</code>&gt;&gt;<code>bootstrapLoader</code></p>
<h2 id="案例分析"><a href="#案例分析" class="headerlink" title="案例分析"></a>案例分析</h2><p>有一次我在编写一个sdk,打算把它打包成jar,给另外的web应用使用,姑且叫该项目为B工程;<br>A工程(web项目)依赖B工程,在B工程中的classpath下有个配置文件plugins.xml,同时在A工程的classpath下也有一个同名的配置文件,那么<strong>A工程下的配置文件则会覆盖B下的文件,同样包路径的class文件也会覆盖</strong>。</p>
<p>所以,如果我在A工程下写一个配置文件<code>plugins.xml</code>,那么它打包成war文件的时候会覆盖B工程jar包里面的配置文件<code>plugins.xml</code>,然后在glassfish上运行war包的时候会被<br>WebAppClassLoader加载(如果是java项目则会被AppClassloader获取到)</p>
<p>下面是项目中用到的部分代码:<br><img src="/uploads/ClassLoader1.png" alt="读取配置文件"></p>
<p><img src="/uploads/ClassLoader2.png" alt="深挖代码1"></p>
<p>这里解释下,因为BootstrapLoader是jvm底层实现,没有具体实现的Class类,所以如果获取到的ClassLoader为null,则表示是由BootstrapLoader加载的;<br>所以这里的逻辑为:如果是bootstraploader,但是因为bootstraploader下肯定没有资源文件,就获取systemloader下的资源文件<br>如果不是bootstraploader,就直接取得classloader然后调用getResourceAsStream方法</p>
<p><img src="/uploads/ClassLoader3.png" alt="深挖代码2"></p>
<p><img src="/uploads/ClassLoader4.png" alt="深挖代码3"></p>
<p>这里的findResource是每个继承了ClassLoader的类可以自行实现的方法,是为了自定义自己独特的加载方法.</p>
<p>getResource方法,是委托模型,先遍历到最顶部的ClassLoader(即bootstrapClassLoader),如果找到直接返回url,如果找不到,就找extClassLoader,这样逐渐遍历.</p>
<p>逻辑是这样的,从WebappClassLoader开始找,儿子让父亲去找,父亲让自己的父亲去找…到了bootstrapClassLoader了,自己都是祖先了,没有父亲可以找了,所以调用自己的getBootstrapResource方法找,找不到,再继续调用自己独特的方法findResource找,还是找不到,只能返回null给儿子,儿子说”父亲找不到,只能自己找了”,所以调用自己独特的方法findResource去找,结果也找不到,再继续交给儿子…直到后面WebappClassLoader自己找到了…你说为什么WebappClassLoader一开始不自己找呢,这就是所谓的委托模型…</p>
<blockquote>
<p>依次遍历的顺序如下:<br>org.glassfish.web.loader.WebappClassLoader<br>org.glassfish.internal.api.DelegatingClassLoader<br>java.net.URLClassLoader<br>APIClassLoader<br>ExtClassLoader——ext下的jar里面<br>bootstrapLoader——–jre&#x2F;classes下</p>
</blockquote>
<p>看下图可能更理解点,我把配置文件放在classpath的包里面,然后最外层的项目没有配置文件:<br><img src="/uploads/ClassLoader5.png" alt="深挖代码4"></p>
<h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><blockquote>
<p><a href="http://blog.csdn.net/lyjshen/article/details/52421554">http://blog.csdn.net/lyjshen/article/details/52421554</a></p>
</blockquote>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>ClassLoader</tag>
      </tags>
  </entry>
  <entry>
    <title>MySQL开发实战笔记</title>
    <url>/article/11695.html</url>
    <content><![CDATA[<blockquote>
<p>本文记录了 MySQL 开发中常见的问题与解决方案，包括索引优化、事务处理、死锁分析等实战经验。</p>
</blockquote>
<h2 id="DDL-与-DML"><a href="#DDL-与-DML" class="headerlink" title="DDL 与 DML"></a>DDL 与 DML</h2><table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>DDL</td>
<td>数据定义语言，如 CREATE、ALTER、DROP 等，会超时</td>
</tr>
<tr>
<td>DML</td>
<td>数据操作语言，如 SELECT、INSERT、UPDATE、DELETE，会阻塞直到执行完成</td>
</tr>
</tbody></table>
<h2 id="优化技巧"><a href="#优化技巧" class="headerlink" title="优化技巧"></a>优化技巧</h2><h3 id="查询语句优化"><a href="#查询语句优化" class="headerlink" title="查询语句优化"></a>查询语句优化</h3><ol>
<li><strong>尽量用 JOIN 查询</strong>，左表是小表去 LEFT JOIN</li>
<li>效率低的场景：能用 EXISTS（小表）就不用 IN（大表）</li>
<li>用 <code>SQL_NO_CACHE</code> 查看执行计划，排除缓存干扰</li>
</ol>
<h3 id="索引优化"><a href="#索引优化" class="headerlink" title="索引优化"></a>索引优化</h3><ol>
<li><strong>组合索引</strong> <code>(a,b,c)</code> 查询只要带上 <code>a</code> 就走索引</li>
<li>组合索引 <code>(a,b,c)</code> 和 <code>(a,c,d)</code> 可以合并为 <code>(a,b,c,d)</code></li>
<li>唯一组合索引跟普通组合索引用法一样，只是多了唯一约束</li>
<li>如果一个字段包含很多重复值，走索引意义不大，几乎是全表扫描</li>
</ol>
<h3 id="大数据量操作"><a href="#大数据量操作" class="headerlink" title="大数据量操作"></a>大数据量操作</h3><p>当需要 UPDATE&#x2F;DELETE 大量数据时，为避免大事务问题：</p>
<ol>
<li><strong>方案一</strong>：把匹配条件的记录 ID 查询出来，放入 Redis，用游标分批处理</li>
<li><strong>方案二</strong>：查询出最大 ID 和最小 ID，分段处理</li>
</ol>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 示例：分段删除</span></span><br><span class="line"><span class="keyword">DELETE</span> <span class="keyword">FROM</span> <span class="keyword">table</span> <span class="keyword">WHERE</span> id <span class="keyword">BETWEEN</span> ? <span class="keyword">AND</span> ? <span class="keyword">AND</span> <span class="keyword">condition</span>;</span><br></pre></td></tr></table></figure>

<p>当需要SELECT时需要防止没有带条件查询所有记录，导致内存溢出。</p>
<h2 id="事务与并发问题"><a href="#事务与并发问题" class="headerlink" title="事务与并发问题"></a>事务与并发问题</h2><h3 id="事务未提交导致查询不到数据"><a href="#事务未提交导致查询不到数据" class="headerlink" title="事务未提交导致查询不到数据"></a>事务未提交导致查询不到数据</h3><p><strong>问题场景</strong>：流程落地后需要推送数据到核算系统，但推送线程查询不到刚落地数据。</p>
<p><strong>原因</strong>：启动线程太快，事务还未完全提交就开始查询。</p>
<p><strong>解决方案</strong>：使用 <code>TransactionSynchronizationManager</code> 在事务提交后执行：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">TransactionSynchronizationManager.registerSynchronization(</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">TransactionSynchronizationAdapter</span>() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterCommit</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 事务提交后执行推送逻辑</span></span><br><span class="line">            calculateStandardService.createIncreaseCalculateLedgerWithThread(ledgerList);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<h2 id="死锁问题"><a href="#死锁问题" class="headerlink" title="死锁问题"></a>死锁问题</h2><h3 id="死锁产生原因"><a href="#死锁产生原因" class="headerlink" title="死锁产生原因"></a>死锁产生原因</h3><p>锁只有在竞争一个以上的资源时才会产生，MySQL 死锁都是基于索引的。</p>
<h3 id="典型死锁场景"><a href="#典型死锁场景" class="headerlink" title="典型死锁场景"></a>典型死锁场景</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 事务A</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> a <span class="keyword">VALUES</span> (...);</span><br><span class="line"><span class="keyword">INSERT INTO</span> b <span class="keyword">VALUES</span> (...);</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 事务B</span></span><br><span class="line"><span class="keyword">INSERT INTO</span> b <span class="keyword">VALUES</span> (...);</span><br><span class="line"><span class="keyword">INSERT INTO</span> a <span class="keyword">VALUES</span> (...);</span><br></pre></td></tr></table></figure>

<p>因为表里建了唯一索引，INSERT 之前会锁住索引页，两个事务没提交时会互相死锁。</p>
<h3 id="死锁日志分析"><a href="#死锁日志分析" class="headerlink" title="死锁日志分析"></a>死锁日志分析</h3><h4 id="如何找到死锁日志"><a href="#如何找到死锁日志" class="headerlink" title="如何找到死锁日志"></a>如何找到死锁日志</h4><p>MySQL 发生死锁时，会自动检测并回滚其中一个事务，同时在错误日志中记录死锁信息。</p>
<p>查看方式：<br>方法一：查看最近一次死锁信息<br>SHOW ENGINE INNODB STATUS;</p>
<p>方法二：查看历史死锁日志<br>在 MySQL 配置文件中添加：<br>innodb_print_all_deadlocks &#x3D; 1<br>在MySQL中看错误日志的位置：<br>SHOW VARIABLES LIKE ‘log_error’;<br>找到错误日志并查看 MySQL 错误日志文件:<br>cat error.log</p>
<h4 id="死锁日志结构"><a href="#死锁日志结构" class="headerlink" title="死锁日志结构"></a>死锁日志结构</h4><p>死锁日志通常包含以下部分：</p>
<ol>
<li><strong>TRANSACTION 信息</strong>：每个事务的信息，包括事务ID、活跃时间、SQL语句</li>
<li><strong>WAITING FOR THIS LOCK TO BE GRANTED</strong>：事务正在等待的锁</li>
<li><strong>HOLDS THE LOCK(S)</strong>：事务持有的锁</li>
<li><strong>WE ROLL BACK TRANSACTION</strong>：回滚的事务</li>
</ol>
<h4 id="常见锁类型及含义"><a href="#常见锁类型及含义" class="headerlink" title="常见锁类型及含义"></a>常见锁类型及含义</h4><table>
<thead>
<tr>
<th>锁类型</th>
<th>含义</th>
</tr>
</thead>
<tbody><tr>
<td><code>lock_mode X</code></td>
<td>排他锁（Exclusive Lock），阻止其他事务读写</td>
</tr>
<tr>
<td><code>lock_mode S</code></td>
<td>共享锁（Shared Lock），允许其他事务读但不能写</td>
</tr>
<tr>
<td><code>lock_mode X locks rec but not gap</code></td>
<td>记录锁，锁住具体行记录</td>
</tr>
<tr>
<td><code>lock_mode X locks gap before rec</code></td>
<td>间隙锁（Gap Lock），锁住记录之间的间隙</td>
</tr>
<tr>
<td><code>lock_mode X insert intention</code></td>
<td>插入意向锁，INSERT 操作前获取</td>
</tr>
<tr>
<td><code>RECORD LOCKS</code></td>
<td>行级锁，锁住具体的行记录</td>
</tr>
<tr>
<td><code>lock_struct(s)</code></td>
<td>锁结构的数量</td>
</tr>
<tr>
<td><code>row lock(s)</code></td>
<td>行锁的数量</td>
</tr>
<tr>
<td><code>undo log entries</code></td>
<td>undo 日志条目数</td>
</tr>
</tbody></table>
<h4 id="死锁日志示例"><a href="#死锁日志示例" class="headerlink" title="死锁日志示例"></a>死锁日志示例</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">1</span>) TRANSACTION:</span><br><span class="line">TRANSACTION <span class="number">125647805</span>, ACTIVE <span class="number">0</span> sec inserting</span><br><span class="line">mysql tables <span class="keyword">in</span> use <span class="number">1</span>, locked <span class="number">1</span></span><br><span class="line">LOCK WAIT <span class="number">12</span> lock struct(s), heap size <span class="number">2936</span>, <span class="number">12</span> <span class="type">row</span> lock(s), undo log entries <span class="number">214</span></span><br><span class="line">MySQL thread id <span class="number">2288831</span>, query id <span class="number">88578026</span> updating</span><br><span class="line"><span class="keyword">INSERT INTO</span> t_cnb_calculation_standard (...) <span class="keyword">VALUES</span> (...)</span><br><span class="line"></span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">1</span>) WAITING <span class="keyword">FOR</span> THIS LOCK <span class="keyword">TO</span> BE GRANTED:</span><br><span class="line">RECORD LOCKS space id <span class="number">1335</span> page <span class="keyword">no</span> <span class="number">393786</span> n bits <span class="number">200</span></span><br><span class="line">index `Index_calculation_standard_unique_edc` <span class="keyword">of</span> <span class="keyword">table</span> `ihr_pro_salary`.`t_cnb_calculation_standard`</span><br><span class="line">trx id <span class="number">125647805</span> lock_mode X <span class="keyword">insert</span> intention waiting</span><br><span class="line"></span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">2</span>) TRANSACTION:</span><br><span class="line">TRANSACTION <span class="number">125647804</span>, ACTIVE <span class="number">0</span> sec inserting</span><br><span class="line">mysql tables <span class="keyword">in</span> use <span class="number">1</span>, locked <span class="number">1</span></span><br><span class="line"><span class="number">39</span> lock struct(s), heap size <span class="number">6544</span>, <span class="number">45</span> <span class="type">row</span> lock(s), undo log entries <span class="number">573</span></span><br><span class="line"></span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">2</span>) HOLDS THE LOCK(S):</span><br><span class="line">RECORD LOCKS space id <span class="number">1335</span> page <span class="keyword">no</span> <span class="number">393786</span> n bits <span class="number">200</span></span><br><span class="line">index `Index_calculation_standard_unique_edc` <span class="keyword">of</span> <span class="keyword">table</span> `ihr_pro_salary`.`t_cnb_calculation_standard`</span><br><span class="line">trx id <span class="number">125647804</span> lock mode S</span><br><span class="line"></span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> (<span class="number">2</span>) WAITING <span class="keyword">FOR</span> THIS LOCK <span class="keyword">TO</span> BE GRANTED:</span><br><span class="line">RECORD LOCKS space id <span class="number">1335</span> page <span class="keyword">no</span> <span class="number">38913</span> n bits <span class="number">280</span></span><br><span class="line">index `Index_calculation_standard_unique_edc` trx id <span class="number">125647804</span> lock_mode S waiting</span><br><span class="line"></span><br><span class="line"><span class="operator">*</span><span class="operator">*</span><span class="operator">*</span> WE ROLL BACK TRANSACTION (<span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<p><strong>日志解读：</strong></p>
<ul>
<li>事务1 正在执行 INSERT，正在等待获取<strong>插入意向锁</strong></li>
<li>事务2 持有<strong>共享锁（S锁）</strong>，同时在等待另一个位置的共享锁</li>
<li>事务1 和 事务2 形成循环等待，MySQL 检测到死锁，回滚了事务1</li>
</ul>
<h2 id="MySQL-基础架构"><a href="#MySQL-基础架构" class="headerlink" title="MySQL 基础架构"></a>MySQL 基础架构</h2><h3 id="Server-层-vs-存储引擎层"><a href="#Server-层-vs-存储引擎层" class="headerlink" title="Server 层 vs 存储引擎层"></a>Server 层 vs 存储引擎层</h3><p><img src="/uploads/MySQL%E7%9A%84%E5%9F%BA%E6%9C%AC%E6%9E%B6%E6%9E%84.png" alt="MySQL的基本架构"></p>
<p>MySQL 整体分为两层：</p>
<ul>
<li><strong>Server 层</strong>：连接器、查询缓存、分析器、优化器、执行器</li>
<li><strong>存储引擎层</strong>：负责数据的存储和提取（插件式，支持 InnoDB、MyISAM 等）</li>
</ul>
<h3 id="执行流程"><a href="#执行流程" class="headerlink" title="执行流程"></a>执行流程</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">客户端 → 连接器 → 查询缓存 → 分析器 → 优化器 → 执行器 → 存储引擎</span><br></pre></td></tr></table></figure>

<ol>
<li><strong>连接器</strong>：管理连接权限，定期断开长连接</li>
<li><strong>查询缓存</strong>：表更新会导致缓存失效，建议关闭</li>
<li><strong>分析器</strong>：词法分析和语法分析</li>
<li><strong>优化器</strong>：决定使用哪个索引、多表连接顺序</li>
<li><strong>执行器</strong>：表权限检查，调用存储引擎接口</li>
</ol>
<h3 id="日志系统"><a href="#日志系统" class="headerlink" title="日志系统"></a>日志系统</h3><h4 id="redo-log（重放日志）"><a href="#redo-log（重放日志）" class="headerlink" title="redo log（重放日志）"></a>redo log（重放日志）</h4><p><strong>作用</strong>：InnoDB 特有，保证 crash-safe 能力</p>
<p><strong>特点</strong>：</p>
<ul>
<li>物理日志，记录”在某个数据页上做了什么修改”</li>
<li>循环写，空间固定</li>
<li>固定大小（如 4 个 1GB 文件）</li>
</ul>
<p><strong>原理</strong>：相当于”粉板”，write pos 记录当前位置，checkpoint 记录擦除位置。</p>
<p>InnoDB 的 redo log 是固定大小的，比如可以配置为一组 4 个文件，每个文件的大小是 1GB，那么这块“粉板”总共就可以记录 4GB 的操作。从头开始写，写到末尾就又回到开头循环写，如下面这个图所示:<br><img src="/uploads/redo_log%E6%97%A5%E5%BF%97%E5%8E%9F%E7%90%86.png" alt="redo_log日志原理"></p>
<p>write pos 是当前记录的位置，一边写一边后移，写到第 3 号文件末尾后就回到 0 号文件开头。checkpoint 是当前要擦除的位置，也是往后推移并且循环的，擦除记录前要把记录更新到数据文件。</p>
<p>write pos 和 checkpoint 之间的是“粉板”上还空着的部分，可以用来记录新的操作。如果 write pos 追上 checkpoint，表示“粉板”满了，这时候不能再执行新的更新，得停下来先擦掉一些记录，把 checkpoint 推进一下。</p>
<h4 id="binlog（归档日志）"><a href="#binlog（归档日志）" class="headerlink" title="binlog（归档日志）"></a>binlog（归档日志）</h4><p><strong>作用</strong>：Server 层日志，用于数据恢复和主从复制</p>
<p><strong>特点</strong>：</p>
<ul>
<li>逻辑日志，记录原始逻辑（如”给 ID&#x3D;2 加 1”）</li>
<li>追加写，不会覆盖</li>
<li>可用于数据恢复</li>
</ul>
<h4 id="两阶段提交"><a href="#两阶段提交" class="headerlink" title="两阶段提交"></a>两阶段提交</h4><p>简单流程图：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">更新语句 → 执行器 → 存储引擎</span><br><span class="line">                    ↓</span><br><span class="line">                 写 redo log (prepare 状态)</span><br><span class="line">            ↓</span><br><span class="line">         写 binlog</span><br><span class="line">                    ↓</span><br><span class="line">         提交事务 (redo log commit)</span><br></pre></td></tr></table></figure>

<p>这样能保证两个日志一致，崩溃恢复时可以根据状态恢复数据。</p>
<p>包含数据写入的完整流程图：</p>
<pre><code class="highlight mermaid">sequenceDiagram
    participant 执行器 as Server层 (执行器)
    participant InnoDB as InnoDB 存储引擎
    participant BP as Buffer Pool (内存)
    participant redo as Redo Log (磁盘)
    participant binlog as Binlog (磁盘)
    participant disk as 数据文件(磁盘)

    Note over 执行器,disk: 事务执行阶段（提交前）
    执行器-&gt;&gt;InnoDB: 1. 执行 INSERT/UPDATE/DELETE
    InnoDB-&gt;&gt;BP: 2. 查找需要的数据页
    alt 数据页已在 Buffer Pool 中
        BP--&gt;&gt;InnoDB: 返回内存页地址
    else 数据页不在 Buffer Pool
        InnoDB-&gt;&gt;disk: 3. 从磁盘读取数据页
        disk--&gt;&gt;InnoDB: 返回数据页
        InnoDB-&gt;&gt;BP: 4. 将数据页加载到 Buffer Pool
    end
    InnoDB-&gt;&gt;BP: 5. 在内存中修改数据页（产生脏页）&lt;br/&gt;同时生成 redo log buffer
    BP--&gt;&gt;InnoDB: 修改完成
    InnoDB--&gt;&gt;执行器: 6. 返回修改完成

    Note over 执行器,disk: 事务提交阶段（两阶段提交）
    执行器-&gt;&gt;InnoDB: 7. 准备提交 (prepare)
    InnoDB-&gt;&gt;redo: 8. 将 redo log buffer 刷盘&lt;br/&gt;（状态为 prepare）
    InnoDB--&gt;&gt;执行器: 准备完成

    执行器-&gt;&gt;binlog: 9. 写入 binlog 并刷盘
    binlog--&gt;&gt;执行器: binlog 写入完成

    执行器-&gt;&gt;InnoDB: 10. 提交事务 (commit)
    InnoDB-&gt;&gt;redo: 11. 将 redo log 状态改为 commit&lt;br/&gt;（并刷盘）
    InnoDB--&gt;&gt;执行器: 提交完成

    Note over 执行器,disk: 异步刷盘（后台线程）
    InnoDB-&gt;&gt;disk: 12. 后续将脏页写回数据文件

    Note over 执行器,disk: 崩溃恢复流程（数据库重启时）
    rect rgb(240, 240, 240)
        Note left of redo: 扫描 redo log 中所有 prepare 状态的事务
        alt 该事务在 binlog 中存在完整记录
            Note over InnoDB: 恢复时认为该事务已提交&lt;br/&gt;重放 redo log 中的修改
            InnoDB-&gt;&gt;redo: 将事务状态改为 commit
            InnoDB-&gt;&gt;BP: 必要时将修改应用到 Buffer Pool
        else 该事务在 binlog 中无记录或不完整
            Note over InnoDB: 恢复时回滚该事务
            InnoDB-&gt;&gt;redo: 不提交，后续清理
        end
    end</code></pre>

<hr>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li><strong>查询优化</strong>：多用 JOIN，合理使用索引，避免全表扫描</li>
<li><strong>大事务</strong>：分批处理，避免长时间锁表</li>
<li><strong>并发问题</strong>：注意事务提交时机，避免查询不到刚写入的数据</li>
<li><strong>死锁分析</strong>：基于索引产生，分析锁等待关系</li>
<li><strong>日志理解</strong>：redo log 保证持久化，binlog 用于恢复和复制</li>
</ol>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>MySQL</tag>
        <tag>数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>Java基础</title>
    <url>/article/45792.html</url>
    <content><![CDATA[<h2 id="public-private-protected-及不写时的区别"><a href="#public-private-protected-及不写时的区别" class="headerlink" title="public ,private ,protected 及不写时的区别"></a>public ,private ,protected 及不写时的区别</h2><p><code>注意:不写时默认为friendly</code><br>各个作用域所起的作用如下:</p>
<table>
<thead>
<tr>
<th>作用域</th>
<th>当前类</th>
<th>同一package</th>
<th>子孙类</th>
<th>其他package</th>
</tr>
</thead>
<tbody><tr>
<td>public</td>
<td>√</td>
<td>√</td>
<td>√</td>
<td>√</td>
</tr>
<tr>
<td>protected</td>
<td>√</td>
<td>√</td>
<td>√</td>
<td>×</td>
</tr>
<tr>
<td>friendly</td>
<td>√</td>
<td>√</td>
<td>×</td>
<td>×</td>
</tr>
<tr>
<td>private</td>
<td>√</td>
<td>×</td>
<td>×</td>
<td>×</td>
</tr>
</tbody></table>
<h2 id="为什么编译出的class的文件名有些带有-1-2这种"><a href="#为什么编译出的class的文件名有些带有-1-2这种" class="headerlink" title="为什么编译出的class的文件名有些带有$1,$2这种"></a>为什么编译出的class的文件名有些带有$1,$2这种</h2><p>Those are the <code>.class</code> files that hold the anonymous inner classes.<br>参考资料:<br><a href="https://stackoverflow.com/questions/1075207/what-are-the-1-in-class-file">https://stackoverflow.com/questions/1075207/what-are-the-1-in-class-file</a></p>
<h2 id="Java不允许只有返回类型不同的方法重载"><a href="#Java不允许只有返回类型不同的方法重载" class="headerlink" title="Java不允许只有返回类型不同的方法重载"></a>Java不允许只有返回类型不同的方法重载</h2><p>在<code>同一个类</code>中，无论返回值、访问修饰符是否相同，方法名必须相同，参数列表必须不参相同。</p>
<p>因为Java语言层面上<code>overload resolution</code>只看方法名和参数列表的类型，不看返回值类型。<br>如果<code>overload resolution</code>发现方法名+参数列表找到了多个版本匹配度一样，那<code>overload resolution</code>就以“冲突”失败。</p>
<p>在JVM层面上，其实是允许同一个类里有多个同名同参数的方法只在返回值类型上不同。但这个特点没有暴露给Java语言（甚至反射API也处理不了这个特点，只有新的MethodHandle API能处理）。</p>
<h2 id="协变式覆盖和泛型重载"><a href="#协变式覆盖和泛型重载" class="headerlink" title="协变式覆盖和泛型重载"></a>协变式覆盖和泛型重载</h2><h3 id="协变式覆盖"><a href="#协变式覆盖" class="headerlink" title="协变式覆盖"></a>协变式覆盖</h3><p>只要子类方法与超类方法具有相同的方法签名，或者子类方法的返回值是超类方法的子类型(增加了对协变返回值的支持)，就可以覆盖。</p>
<h3 id="泛型重载"><a href="#泛型重载" class="headerlink" title="泛型重载"></a>泛型重载</h3><p>JDK7、8是不可以编译的，需要用JDK6才可以<br>泛型擦除指的是:比如<code>List&lt;String&gt;</code>和<code>List&lt;Integer&gt;</code>,擦除后都变成<code>List</code><br>如果两个泛型方法在擦除泛型信息后，如果只是具有相同的参数类型，而返回值不一样，就可以进行重载；</p>
<p><img src="/uploads/%E6%B3%9B%E5%9E%8B%E8%A6%86%E7%9B%96.png" alt="泛型覆盖"></p>
<h2 id="enum类型的弊端"><a href="#enum类型的弊端" class="headerlink" title="enum类型的弊端"></a>enum类型的弊端</h2><p>使用枚举类型进行映射有一个弊端就是,将来有一天修改了枚举类型的定义,<code>会造成数据库中的数据无法被加载</code>,所以如果对改动的枚举定义(如顺序调整了– ORDINAL;或名称改了;或增减了选项)时一定要同步update数据库中的记录,这对于产品数据库也是个麻烦事. 这一点上还是需要谨慎的思考是否真要用 Java 的枚举值来映射.<br>而用 Integer 或 String 来作为 Java 的属性时则不会造成数据加载的异常,顶多是数据混乱,或有些值无法理解而已.<br>所以,对于一些常用而又量不大的数据,可以一次性把它的映射关系加载进缓存里面,可以考虑用guava–谷歌开源一个jar,其中的BiMap很好用,但是要注意右边要保证唯一性,比如下面这个不能同时是22L</p>
<p><img src="/uploads/bimap%E4%BE%8B%E5%AD%901.png" alt="bimap例子1"></p>
<p>还有下面这个例子,左边的key重复了,会以后面执行的为主:</p>
<p><img src="/uploads/bimap%E4%BE%8B%E5%AD%902.png" alt="bimap例子2"></p>
<h2 id="Map基础知识"><a href="#Map基础知识" class="headerlink" title="Map基础知识"></a>Map基础知识</h2><h3 id="HashMap-底层实现原理"><a href="#HashMap-底层实现原理" class="headerlink" title="HashMap 底层实现原理"></a>HashMap 底层实现原理</h3><p>HashMap 是 Java 中最常用的集合之一，它底层是基于<strong>数组 + 链表&#x2F;红黑树</strong>实现的。</p>
<h4 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h4><p>当你调用 <code>put(key, value)</code> 时：</p>
<ol>
<li><strong>计算哈希值</strong>：先用 <code>hashCode()</code> 计算 key 的哈希值</li>
<li><strong>定位数组位置</strong>：通过哈希算法算出该 key 应该放在数组的哪个位置</li>
<li><strong>存入数组</strong>：如果该位置为空，直接放入</li>
<li><strong>处理冲突</strong>：如果该位置已有元素（哈希冲突），会用链表连接起来；如果链表太长（超过8个），会自动转成红黑树提高查询效率</li>
</ol>
<p>当你调用 <code>get(key)</code> 时：</p>
<ol>
<li>同样先计算 key 的哈希值</li>
<li>找到数组对应的位置</li>
<li>在链表&#x2F;红黑树中查找对应的 Entry</li>
<li>返回 value</li>
</ol>
<h4 id="容量和负荷系数"><a href="#容量和负荷系数" class="headerlink" title="容量和负荷系数"></a>容量和负荷系数</h4><ul>
<li><strong>默认初始容量</strong>：16</li>
<li><strong>默认负荷系数</strong>：0.75</li>
</ul>
<p>当 HashMap 中的元素数量超过 <code>容量 × 负荷系数</code> 时，会进行<strong>扩容</strong>，容量翻倍。</p>
<blockquote>
<p>小技巧：如果你知道大概要存储多少数据，可以提前设置合适的初始容量，避免频繁扩容带来的性能损耗。</p>
</blockquote>
<h4 id="equals-和-hashCode-规则"><a href="#equals-和-hashCode-规则" class="headerlink" title="equals() 和 hashCode() 规则"></a>equals() 和 hashCode() 规则</h4><p>这两个方法是 HashMap 工作的基础，必须遵守以下规则：</p>
<h5 id="规则一：相等的对象必须有相等的哈希值"><a href="#规则一：相等的对象必须有相等的哈希值" class="headerlink" title="规则一：相等的对象必须有相等的哈希值"></a>规则一：相等的对象必须有相等的哈希值</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">如果 a.equals(b) 为 <span class="literal">true</span>，那么 a.hashCode() == b.hashCode() 必须为 <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>这是因为 HashMap 先用 hashCode() 定位，如果哈希值不同就直接认为是不同的元素，不会调用 equals()。</p>
<h5 id="规则二：哈希值相等不一定equals"><a href="#规则二：哈希值相等不一定equals" class="headerlink" title="规则二：哈希值相等不一定equals"></a>规则二：哈希值相等不一定equals</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">a.hashCode() == b.hashCode()，并不代表 a.equals(b) 为 <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>这是因为哈希算法可能会让不同的 key 产生相同的哈希值（哈希冲突），HashMap 会用 equals() 来最终确认。</p>
<h4 id="实际应用"><a href="#实际应用" class="headerlink" title="实际应用"></a>实际应用</h4><p>如果你使用自定义类作为 HashMap 的 key，必须重写这两个方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">equals</span><span class="params">(Object o)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">this</span> == o) <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">if</span> (o == <span class="literal">null</span> || getClass() != o.getClass()) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        <span class="type">Person</span> <span class="variable">person</span> <span class="operator">=</span> (Person) o;</span><br><span class="line">        <span class="keyword">return</span> age == person.age &amp;&amp; Objects.equals(name, person.name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">hashCode</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Objects.hash(name, age);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="如何让-Map-不可变"><a href="#如何让-Map-不可变" class="headerlink" title="如何让 Map 不可变"></a>如何让 Map 不可变</h3><p>即便把 Map 声明为 <code>final</code>，仍然可以添加或删除元素：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这样仍然可以修改！</span></span><br><span class="line"><span class="keyword">final</span> Map&lt;String, String&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">map.put(<span class="string">&quot;key&quot;</span>, <span class="string">&quot;value&quot;</span>);  <span class="comment">// 可以执行</span></span><br></pre></td></tr></table></figure>

<p>正确的不可变 Map 写法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Constants</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Map&lt;Integer, String&gt; STATUS_MAP;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        Map&lt;Integer, String&gt; tempMap = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">        tempMap.put(<span class="number">1</span>, <span class="string">&quot;成功&quot;</span>);</span><br><span class="line">        tempMap.put(<span class="number">2</span>, <span class="string">&quot;失败&quot;</span>);</span><br><span class="line">        <span class="comment">// 使用 Collections.unmodifiableMap() 包装</span></span><br><span class="line">        STATUS_MAP = Collections.unmodifiableMap(tempMap);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样尝试修改时会抛出 <code>UnsupportedOperationException</code> 异常。</p>
<h3 id="Map的其他实现类"><a href="#Map的其他实现类" class="headerlink" title="Map的其他实现类"></a>Map的其他实现类</h3><p>跟HashMap同样实现Map接口的有：HashMap,LinkedHashMap与TreeMap</p>
<p>区别：<br>HashMap不保证数据有序，LinkedHashMap保证数据可以保持插入顺序，而如果我们希望Map可以保持key的大小顺序的时候，我们就需要利用TreeMap了。</p>
<h2 id="异常的分类"><a href="#异常的分类" class="headerlink" title="异常的分类"></a>异常的分类</h2><ol>
<li>异常的继承结构：基类为Throwable，Error和Exception继承Throwable，RuntimeException和IOException等继承Exception，具体的RuntimeException继承RuntimeException。</li>
<li>Error和RuntimeException及其子类成为未检查异常（unchecked），其它异常成为已检查异常（checked）。</li>
</ol>
<p><img src="/uploads/%E5%BC%82%E5%B8%B8%E7%B1%BB%E7%9A%84%E5%85%B3%E8%81%94.png" alt="异常类的关联"></p>
<h2 id="性能问题排查"><a href="#性能问题排查" class="headerlink" title="性能问题排查"></a>性能问题排查</h2><p>先用 <code>jvisualvm.exe</code> 查看内存和线程情况：</p>
<ol>
<li><strong>内存问题</strong>：如果内存太高，右键 dump 出 hprof 文件，用 MAT 分析</li>
<li><strong>线程问题</strong>：如果线程数太多，规范线程池名称，打印堆栈，检查有无死锁</li>
</ol>
<h2 id="性能指标"><a href="#性能指标" class="headerlink" title="性能指标"></a>性能指标</h2><p>QPS &#x3D; req&#x2F;sec &#x3D; 请求数&#x2F;秒【QPS计算PV和机器的方式】</p>
<p>QPS统计方式 [一般使用 http_load 进行统计]QPS &#x3D; 总请求数 &#x2F; ( 进程总数 *   请求时间 )</p>
<p>QPS: 单个进程每秒请求服务器的成功次数</p>
<p>单台服务器每天PV计算<br>公式1：每天总PV &#x3D; QPS * 3600 * 6<br>公式2：每天总PV &#x3D; QPS * 3600 * 8</p>
<p>服务器计算服务器数量 &#x3D;   ceil( 每天总PV &#x2F; 单台服务器每天总PV )</p>
<p>【峰值QPS和机器计算公式】<br>原理：每天80%的访问集中在20%的时间里，这20%时间叫做峰值时间<br>公式：( 总PV数 * 80% ) &#x2F; ( 每天秒数 * 20% ) &#x3D; 峰值时间每秒请求数(QPS)<br>机器：峰值时间每秒QPS &#x2F; 单台机器的QPS   &#x3D; 需要的机器</p>
<p>问：每天300w PV 的在单台机器上，这台机器需要多少QPS？<br>答：( 3000000 * 0.8 ) &#x2F; (86400 * 0.2 ) &#x3D; 139 (QPS)</p>
<p>问：如果一台机器的QPS是58，需要几台机器来支持？<br>答：139 &#x2F; 58 &#x3D; 3</p>
<p>PV&#x3D;page view<br>TPS&#x3D;transactions per second<br>QPS&#x3D;queries per second<br>RPS&#x3D;requests per second</p>
<p>RPS&#x3D;并发数&#x2F;平均响应时间</p>
<h2 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h2><p><img src="http://yikun.github.io/2015/04/06/Java-TreeMap%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86%E5%8F%8A%E5%AE%9E%E7%8E%B0/"></p>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>Java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo博客搭建与插件改造完全指南</title>
    <url>/article/18588.html</url>
    <content><![CDATA[<blockquote>
<p>经过多次尝试，终于搭建好了自己的博客！本文记录了完整的搭建过程和后期插件改造，让博客更加好用。</p>
</blockquote>
<h2 id="为什么选择-Hexo"><a href="#为什么选择-Hexo" class="headerlink" title="为什么选择 Hexo"></a>为什么选择 Hexo</h2><p>以前用过 WordPress，折腾各种主题和插件，还要维护服务器，后来发现很多技术大牛都把博客放在 GitHub Pages 上——静态博客，无需管理后台，用 Markdown 写文章，简直是程序员的最佳选择！</p>
<h2 id="搭建及改造流程概览"><a href="#搭建及改造流程概览" class="headerlink" title="搭建及改造流程概览"></a>搭建及改造流程概览</h2><p>只需要 5 步就能完成搭建：</p>
<ol>
<li><strong>安装 Node.js</strong> - Hexo 基于 Node.js，需要先安装</li>
<li><strong>安装 Hexo</strong> - 用 npm 安装 Hexo 博客框架</li>
<li><strong>配置 SSH</strong> - 让部署时不需要每次输入密码</li>
<li><strong>部署上线</strong> - 推送到 GitHub Pages</li>
<li><strong>插件改造</strong> - 选择符合自己使用习惯的插件安装并优化hexo博客</li>
</ol>
<h2 id="详细搭建步骤"><a href="#详细搭建步骤" class="headerlink" title="详细搭建步骤"></a>详细搭建步骤</h2><h3 id="安装-Node-js"><a href="#安装-Node-js" class="headerlink" title="安装 Node.js"></a>安装 Node.js</h3><p>Hexo 需要 Node.js 环境。直接去 <a href="https://nodejs.org/">Node.js 官网</a> 下载安装包，安装完成后自带 npm 包管理器。</p>
<h3 id="安装-Hexo"><a href="#安装-Hexo" class="headerlink" title="安装 Hexo"></a>安装 Hexo</h3><p>npm 默认源比较慢，建议换成阿里镜像：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 安装 Hexo 命令行工具（全局安装）</span></span><br><span class="line">npm --registry https://registry.npm.taobao.org install hexo-cli -g</span><br><span class="line"></span><br><span class="line"><span class="comment"># 初始化博客项目</span></span><br><span class="line">hexo init my-blog</span><br><span class="line"><span class="built_in">cd</span> my-blog</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装项目依赖</span></span><br><span class="line">npm --registry https://registry.npm.taobao.org install</span><br></pre></td></tr></table></figure>

<p>安装完成后，启动本地服务器预览：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo server</span><br></pre></td></tr></table></figure>

<p>打开浏览器访问 <code>http://localhost:4000</code>，就能看到博客效果了！</p>
<h3 id="配置-Git-SSH-密钥"><a href="#配置-Git-SSH-密钥" class="headerlink" title="配置 Git SSH 密钥"></a>配置 Git SSH 密钥</h3><p>为了让 Hexo 部署时不需要每次输入密码，需要配置 SSH 密钥。</p>
<p><strong>第一步：配置 Git 用户信息</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global user.email <span class="string">&quot;你的邮箱&quot;</span></span><br><span class="line">git config --global user.name <span class="string">&quot;你的昵称&quot;</span></span><br></pre></td></tr></table></figure>

<p><strong>第二步：生成 SSH 密钥</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -f ~/.ssh/id_rsa.github -C <span class="string">&quot;你的邮箱&quot;</span></span><br></pre></td></tr></table></figure>

<p>一路回车，会在 <code>~/.ssh</code> 目录下生成两个文件：</p>
<ul>
<li><code>id_rsa.github</code> - 私钥（自己保管）</li>
<li><code>id_rsa.github.pub</code> - 公钥（放到 GitHub）</li>
</ul>
<p><strong>第三步：添加到 GitHub</strong></p>
<ol>
<li>复制公钥内容：<code>cat ~/.ssh/id_rsa.github.pub</code></li>
<li>打开 GitHub → Settings → SSH and GPG keys → New SSH key</li>
<li>粘贴公钥，保存</li>
</ol>
<p><strong>第四步：创建 SSH 配置文件</strong></p>
<p>在 <code>~/.ssh</code> 目录下新建 <code>config</code> 文件：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Host github.com</span><br><span class="line">    IdentityFile ~/.ssh/id_rsa.github</span><br><span class="line">    User your-username</span><br></pre></td></tr></table></figure>

<p><strong>第五步：测试连接</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span><br></pre></td></tr></table></figure>

<p>看到 <code>You&#39;ve successfully authenticated</code> 就成功了！</p>
<h3 id="部署到-GitHub-Pages"><a href="#部署到-GitHub-Pages" class="headerlink" title="部署到 GitHub Pages"></a>部署到 GitHub Pages</h3><p><strong>第一步：创建 GitHub 仓库</strong></p>
<p>仓库名格式：<code>你的用户名.github.io</code><br>例如：<code>aitestperson.github.io</code></p>
<p><strong>第二步：配置 Hexo 部署</strong></p>
<p>修改 <code>_config.yml</code>：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repository:</span> <span class="string">git@github.com:aitestperson/aitestperson.github.io.git</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">master</span></span><br></pre></td></tr></table></figure>

<p><strong>第三步：部署</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo clean      <span class="comment"># 清理缓存</span></span><br><span class="line">hexo generate   <span class="comment"># 生成静态页面</span></span><br><span class="line">hexo deploy     <span class="comment"># 部署到 GitHub</span></span><br></pre></td></tr></table></figure>

<p>访问 <code>https://你的用户名.github.io</code> 就能看到博客了！</p>
<h3 id="插件改造"><a href="#插件改造" class="headerlink" title="插件改造"></a>插件改造</h3><p>搭建完成后，我对博客进行了一系列插件改造，让它更加好用。</p>
<h4 id="主题"><a href="#主题" class="headerlink" title="主题"></a>主题</h4><p>使用 <a href="https://theme-next.js.org/">Hexo Theme Next</a> 主题，简洁美观，功能丰富。</p>
<h4 id="链接优化"><a href="#链接优化" class="headerlink" title="链接优化"></a>链接优化</h4><table>
<thead>
<tr>
<th>插件</th>
<th>作用</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td>hexo-abbrlink</td>
<td>简短永久链接</td>
<td><code>article/1234abc.html</code></td>
</tr>
<tr>
<td>hexo-filter-titlebased-link</td>
<td>站内链接</td>
<td><code>[[文章标题]]</code> 自动生成链接</td>
</tr>
<tr>
<td>hexo-filter-image-link</td>
<td>非图片链接转换</td>
<td><code>![](file.pdf)</code> 转为 <code>&lt;a&gt;</code> 标签</td>
</tr>
</tbody></table>
<p><strong>abbrlink 配置</strong></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">permalink:</span> <span class="string">article/:abbrlink.html</span></span><br><span class="line"><span class="attr">abbrlink:</span></span><br><span class="line">  <span class="attr">alg:</span> <span class="string">crc32</span>    <span class="comment"># 算法：crc16 或 crc32</span></span><br><span class="line">  <span class="attr">rep:</span> <span class="string">hex</span>      <span class="comment"># 进制：dec 或 hex</span></span><br></pre></td></tr></table></figure>

<p><strong>站内链接使用</strong></p>
<p>安装 <code>hexo-filter-titlebased-link</code> 后，可以这样引用文章：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">这是 [[另一篇文章]] 的链接</span><br></pre></td></tr></table></figure>

<p><strong>非图片链接转换</strong></p>
<p>自研插件，处理 Markdown 图片语法中的非图片链接：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">image_link_filter:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">imageExtensions:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">jpg</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">png</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">gif</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">webp</span></span><br><span class="line">  <span class="attr">action:</span> <span class="string">convert</span></span><br></pre></td></tr></table></figure>

<p>效果：</p>
<ul>
<li><code>![图片](a.png)</code> → 保持为 <code>&lt;img&gt;</code> 标签</li>
<li><code>![文档](b.pdf)</code> → 转为 <code>&lt;a href=&quot;b.pdf&quot;&gt;文档&lt;/a&gt;</code></li>
</ul>
<h4 id="SEO-优化"><a href="#SEO-优化" class="headerlink" title="SEO 优化"></a>SEO 优化</h4><table>
<thead>
<tr>
<th>插件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>hexo-generator-sitemap</td>
<td>生成 sitemap.xml（谷歌、必应等）</td>
</tr>
<tr>
<td>hexo-generator-baidu-sitemap</td>
<td>生成百度专用站点地图</td>
</tr>
</tbody></table>
<h4 id="分类与标签"><a href="#分类与标签" class="headerlink" title="分类与标签"></a>分类与标签</h4><table>
<thead>
<tr>
<th>插件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>hexo-auto-category</td>
<td>自动生成分类</td>
</tr>
</tbody></table>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">auto_category:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span>  <span class="comment"># 根据文章目录自动生成分类</span></span><br></pre></td></tr></table></figure>

<h4 id="搜索功能"><a href="#搜索功能" class="headerlink" title="搜索功能"></a>搜索功能</h4><table>
<thead>
<tr>
<th>插件</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>hexo-generator-searchdb</td>
<td>本地搜索</td>
</tr>
</tbody></table>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">search:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">search.xml</span></span><br><span class="line">  <span class="attr">field:</span> <span class="string">post</span></span><br><span class="line">  <span class="attr">format:</span> <span class="string">html</span></span><br><span class="line">  <span class="attr">limit:</span> <span class="number">10000</span></span><br></pre></td></tr></table></figure>

<p>配合主题配置，可以在博客中实现即时搜索。</p>
<hr>
<h2 id="插件汇总"><a href="#插件汇总" class="headerlink" title="插件汇总"></a>插件汇总</h2><table>
<thead>
<tr>
<th>插件</th>
<th>版本</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>hexo-abbrlink</td>
<td>^2.2.1</td>
<td>生成简短永久链接</td>
</tr>
<tr>
<td>hexo-auto-category</td>
<td>^0.2.2</td>
<td>自动生成分类</td>
</tr>
<tr>
<td>hexo-filter-image-link</td>
<td>github:aitestperson&#x2F;hexo-filter-image-link</td>
<td>非图片链接转a标签</td>
</tr>
<tr>
<td>hexo-filter-titlebased-link</td>
<td>^0.2.7</td>
<td>站内链接支持</td>
</tr>
<tr>
<td>hexo-generator-sitemap</td>
<td>^3.0.1</td>
<td>站点地图</td>
</tr>
<tr>
<td>hexo-generator-baidu-sitemap</td>
<td>^0.1.9</td>
<td>百度站点地图</td>
</tr>
<tr>
<td>hexo-generator-searchdb</td>
<td>^1.5.0</td>
<td>本地搜索</td>
</tr>
<tr>
<td>hexo-theme-next</td>
<td>^8.27.0</td>
<td>博客主题</td>
</tr>
</tbody></table>
<hr>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>配合这些插件改造后，博客功能更加完善，写作体验也更好。快来试试吧！</p>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>hexo</tag>
        <tag>GithubPages</tag>
        <tag>博客搭建</tag>
      </tags>
  </entry>
  <entry>
    <title>前端常用图表框架</title>
    <url>/article/36228.html</url>
    <content><![CDATA[<blockquote class="blockquote-center">
<p>友情提示:此页面延迟大,自备梯子,效果更佳~~~ </p>

</blockquote>


<h2 id="d3"><a href="#d3" class="headerlink" title="d3"></a>d3</h2><h3 id="柱状图"><a href="#柱状图" class="headerlink" title="柱状图"></a>柱状图</h3>
<script async src="//jsfiddle.net/godtreeGGG/0qx8x0oL/embed/result,js,html,css/"></script>


<h2 id="echarts"><a href="#echarts" class="headerlink" title="echarts"></a>echarts</h2><h3 id="仪表盘"><a href="#仪表盘" class="headerlink" title="仪表盘"></a>仪表盘</h3>
<script async src="//jsfiddle.net/godtreeGGG/waf66rgp/embed/result,js,html/"></script>


<h3 id="折线图"><a href="#折线图" class="headerlink" title="折线图"></a>折线图</h3>
<script async src="//jsfiddle.net/godtreeGGG/xjta89wp/embed/result,js,html/"></script>


<h3 id="折线图2"><a href="#折线图2" class="headerlink" title="折线图2"></a>折线图2</h3>
<script async src="//jsfiddle.net/godtreeGGG/jkewbx66/embed/result,js,html/"></script>


<h3 id="折线图3"><a href="#折线图3" class="headerlink" title="折线图3"></a>折线图3</h3>
<script async src="//jsfiddle.net/godtreeGGG/cme8uhc3/embed/result,js,html/"></script>


<h3 id="折线图-条形图"><a href="#折线图-条形图" class="headerlink" title="折线图&amp;条形图"></a>折线图&amp;条形图</h3>
<script async src="//jsfiddle.net/godtreeGGG/orja0d4r/embed/result,js,html/"></script>


<h3 id="扇形图"><a href="#扇形图" class="headerlink" title="扇形图"></a>扇形图</h3>
<script async src="//jsfiddle.net/godtreeGGG/w7e62xdf/embed/result,js,html/"></script>


<h3 id="柱状图-1"><a href="#柱状图-1" class="headerlink" title="柱状图"></a>柱状图</h3>
<script async src="//jsfiddle.net/godtreeGGG/vh7bjzaw/embed/result,js,html/"></script>


<h3 id="横向柱状图"><a href="#横向柱状图" class="headerlink" title="横向柱状图"></a>横向柱状图</h3>
<script async src="//jsfiddle.net/godtreeGGG/ejt7gs9c/embed/result,js,html/"></script>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>前端开发</tag>
      </tags>
  </entry>
  <entry>
    <title>后端知识体系</title>
    <url>/article/90001.html</url>
    <content><![CDATA[<h1 id="个人后端知识体系整理（完善中）"><a href="#个人后端知识体系整理（完善中）" class="headerlink" title="个人后端知识体系整理（完善中）"></a>个人后端知识体系整理（完善中）</h1><blockquote class="pullquote mindmap mindmap-md"><ul>
<li>中心节点<ul>
<li>数据结构与算法<ul>
<li><a href="https://mp.weixin.qq.com/s?__biz=MzI5ODI5NDkxMw==&mid=2247490825&idx=2&sn=5c26af13d00976295786c65d41f768a6&chksm=eca956e7dbdedff127be68312e384d4c3ff671feb8e4b0867902bbd2b2398291dad80dcf6362&mpshare=1&scene=1&srcid=0630IutraEvQosrBFR6EloKH&sharer_sharetime=1593508521706&sharer_shareid=f433210a053adb795a013ded9465017f&key=69fdb5ac5982b1743f42e86e4d74ca614d6af9885ffa4f960b0a60322c9cdcbf33d4a61e4ff57bbb3a86bed118741c19b63779c95229e4469e22b33626eb69025d0902fe4b23ad66d7a1bcb48127adb1&ascene=1&uin=Mjg5MDEzNzIwNA==&devicetype=Windows+10+x64&version=62090070&lang=zh_CN&exportkey=Axq9aPV8CLjRvbHvh5zppDo=&pass_ticket=3DrEaHrX3hmWfS7vcYGmWF/6kyksnJeIjhsv7+8Azo1X0N8xSy0+Hx8leRIlpnn9">排序算法</a><ul>
<li>比较类排序<ul>
<li>交换排序<ul>
<li>冒泡排序</li>
<li>快速排序</li>
</ul>
</li>
<li>插入排序<ul>
<li>普通插入排序</li>
<li><a href="https://blog.csdn.net/qq_39207948/article/details/80006224">希尔排序</a></li>
</ul>
</li>
<li>选择排序<ul>
<li>普通选择排序</li>
<li>堆排序</li>
</ul>
</li>
<li>归并排序<ul>
<li>二路归并排序</li>
<li>多路归并排序</li>
</ul>
</li>
</ul>
</li>
<li>非比较类排序<ul>
<li>计数排序</li>
<li>桶排序</li>
<li><a href="https://blog.csdn.net/u011948899/article/details/78027838">基数排序</a><ul>
<li>LSD</li>
<li>MSD</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li><a href="https://mp.weixin.qq.com/s?__biz=MzI2NjA3NTc4Ng==&mid=2652083049&idx=1&sn=6ab60b065836ccbb960ac1c9fdf20771&chksm=f174988cc603119a59d141c9a2d8144f0d56d09112d8f68055adeb266f0aa9c526581f2650df&mpshare=1&scene=24&srcid=0625vppe7eiz8AETXmXfGEHg&sharer_sharetime=1593065345010&sharer_shareid=f433210a053adb795a013ded9465017f&key=82c238abbe3b82ba7cfad489ccf47c752ccc359c9faad1d518a2be1b48139aeb73df8cc6f08c6dc5fd19991354ecd39f36c5275ad3446e3d1f61e721576e4ddec9179911575742ae76285226bfae4bbf&ascene=14&uin=Mjg5MDEzNzIwNA==&devicetype=Windows+10+x64&version=62090070&lang=zh_CN&exportkey=A+Aajt567Hs2G6qP8RXDrZc=&pass_ticket=3DrEaHrX3hmWfS7vcYGmWF/6kyksnJeIjhsv7+8Azo1X0N8xSy0+Hx8leRIlpnn9&winzoom=1">树</a><ul>
<li>多路搜索树<ul>
<li>普通多路搜索树</li>
<li>平衡多路搜索树<ul>
<li>B树</li>
<li>B+树</li>
</ul>
</li>
</ul>
</li>
<li>二叉搜索树<ul>
<li>普通二叉搜索树<ul>
<li>BST</li>
</ul>
</li>
<li>平衡二叉搜索树<ul>
<li>AVL</li>
<li>Red-Black-Tree</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>图</li>
</ul>
</li>
<li>计算机网络<ul>
<li>tcp&#x2F;ip四层模型<ul>
<li>tcp<ul>
<li>三次握手,四次挥手</li>
</ul>
</li>
<li>udp</li>
</ul>
</li>
</ul>
</li>
<li>java<ul>
<li>java基础<ul>
<li>多线程<ul>
<li>wait notify</li>
<li>线程池</li>
<li>线程实现</li>
</ul>
</li>
<li>JVM<ul>
<li>判断能否gc<ul>
<li>引用计数算法（Reference Counting）</li>
<li>根搜索算法（GC Root Tracing）</li>
</ul>
</li>
<li>gc类型<ul>
<li>Minor GC</li>
<li>Full GC（Major GC）</li>
</ul>
</li>
<li><a href="https://www.cnblogs.com/aspirant/p/8662690.html">gc回收算法</a><ul>
<li>标记清除（mark-sweep）</li>
<li>标记复制（mark-copy）</li>
<li>标记整理（mark-compact）</li>
<li>分代收集（generational-collection）</li>
</ul>
</li>
<li>内存结构<ul>
<li>堆<ul>
<li><a href="https://www.cnblogs.com/jswang/p/9056038.html">新生代</a><ul>
<li>Eden</li>
<li>S1（Survivor1）</li>
<li>S2（Survivor2）</li>
</ul>
</li>
<li>老年代</li>
</ul>
</li>
<li>方法区</li>
<li>栈</li>
<li>程序计数器</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li>框架源码解析<ul>
<li>spring源码<ul>
<li>aop</li>
<li>ioc<ul>
<li>循环依赖，三级缓存</li>
</ul>
</li>
</ul>
</li>
<li>jdk源码<ul>
<li>hashmap<ul>
<li>hashcode</li>
<li>capity</li>
<li>容积因子</li>
</ul>
</li>
</ul>
</li>
<li>mybatis-spring源码</li>
</ul>
</li>
<li>开源组件<ul>
<li>k8s<ul>
<li>pod</li>
<li>controller</li>
<li>service</li>
<li>integres</li>
</ul>
</li>
<li>redis</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
</ul>
</blockquote><script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>Java基础</tag>
      </tags>
  </entry>
  <entry>
    <title>开发工具技巧</title>
    <url>/article/4352.html</url>
    <content><![CDATA[<h2 id="vim"><a href="#vim" class="headerlink" title="vim"></a>vim</h2><p>最基本的复制是 y，粘贴是 p，剪切是 d。<br>复制和剪切的命令后面都可以接一个移动命令，表示将光标移动区间内的文本复制过来。<br>粘贴时 p 与 P 的区别是贴到光标前后的区别。<br>如果是在插入模式下想粘贴，也可以不退回普通模式，用 <code>ctrl+r再输入*</code> 来粘贴。</p>
<p>在窗格间切换的方法:</p>
<table>
<thead>
<tr>
<th>快捷键或者命令</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>:bd+数字</code></td>
<td>删除指定缓存区的文件</td>
</tr>
<tr>
<td><code>Ctrl+w+方向键</code></td>
<td>切换到前／下／上／后一个窗格</td>
</tr>
<tr>
<td><code>Ctrl+w+h/j/k/l</code></td>
<td>同上</td>
</tr>
<tr>
<td><code>:noh</code></td>
<td>取消高亮</td>
</tr>
</tbody></table>
<h2 id="eclipse"><a href="#eclipse" class="headerlink" title="eclipse"></a>eclipse</h2><p>eclipse快捷键</p>
<blockquote>
<p>ctrl+shift+f快捷键，自动排版<br>ctrl+shift+o快捷键，自动引入包<br>Alt+&#x2F; 代码助手完成一些代码的插入 ，自动显示提示信息</p>
</blockquote>
<h2 id="ps"><a href="#ps" class="headerlink" title="ps"></a>ps</h2><p>Ctrl + Alt + Shift + E  盖印图层 盖印可见图层,即将下面所有图层合并,但各自原图层仍保留<br>Ctrl + Alt + Shift + T  重复上一次ctrl+T的操作(可以累积效果),并且同时结果会放在新的复制图层(ctrl+shift+T,只重复,不复制)</p>
<h2 id="cmder"><a href="#cmder" class="headerlink" title="cmder"></a>cmder</h2><p>中文乱码问题:<br><code>win+ctrl+p</code>打开Settings<br>在Settings &gt; Startup &gt; Environment里添加：<code>set LANG=zh_CN.UTF8</code><br>PS：<br>这样修改过ls可以正确显示中文，但<code>ls |more</code>还是会出现乱码。<br>如果改为<code>set LANG=zh_CN.GBK</code>可以解决这个问题。</p>
<p>因为cat命令读取文件的编码与此有关，如果改为GBK则cat一个UTF8文件会显示乱码，<br>如果改为UTF8则cat一个GBK文件会显示乱码，<br>具体设置还是看你环境中常用编码。</p>
<h2 id="sublime"><a href="#sublime" class="headerlink" title="sublime"></a>sublime</h2><p>对于程序块缩进可用快捷键:<br>←左缩进ctrl+[<br>→右缩进ctrl+]，效果等同tab键<br>tab 往后缩，或者 shift+tab 往前缩</p>
<h2 id="staruml"><a href="#staruml" class="headerlink" title="staruml"></a>staruml</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">下载:http://staruml.io/</span><br><span class="line">破解方法:http://blog.csdn.net/mergades/article/details/46662413</span><br></pre></td></tr></table></figure>
<h2 id="Windows下带有空格的目录路径"><a href="#Windows下带有空格的目录路径" class="headerlink" title="Windows下带有空格的目录路径"></a>Windows下带有空格的目录路径</h2><p><code>set JAVA_HOME=C:\PROGRA~1\Java\jdk1.8.0_111</code><br>可以防止空格异常</p>
<h2 id="直接读取远程服务器的文件作为参数给某个命令使用"><a href="#直接读取远程服务器的文件作为参数给某个命令使用" class="headerlink" title="直接读取远程服务器的文件作为参数给某个命令使用"></a>直接读取远程服务器的文件作为参数给某个命令使用</h2><p>先下载wget到cmder&#x2F;bin目录下:<br><a href="https://eternallybored.org/misc/wget/">https://eternallybored.org/misc/wget/</a><br>然后直接远程读取:<br><code>wget -O- -q http://algs4.cs.princeton.edu/32bst/tinyST.txt | cat</code></p>
<h2 id="将-Markdown-转成漂亮的脑图"><a href="#将-Markdown-转成漂亮的脑图" class="headerlink" title="将 Markdown 转成漂亮的脑图"></a>将 Markdown 转成漂亮的脑图</h2><p>MarkRemap   有各个平台的版本，开源<br>还可以结合hexo的 <a href="https://github.com/HunterXuan/hexo-simple-mindmap">https://github.com/HunterXuan/hexo-simple-mindmap</a> 插件</p>
<h2 id="其他实用工具"><a href="#其他实用工具" class="headerlink" title="其他实用工具"></a>其他实用工具</h2><blockquote>
<p>U盘资料还原软件:badcopy<br>视频桌面软件:DreamScene<br>flash反编译:Sothink SWF Decompiler<br>cmd替代品:<a href="http://www.jianshu.com/p/b691b48bcee3">cmder</a><br>用文字画uml图:plantuml<br>保存剪切板里的图片到本地文件:Screenshot2Disk<br>截图工具:FastStone,Sharex<br>打开dll文件:Resource Hacker<br>windows下的软件包管理工具:Chocolatey<br>虚拟机管理工具:Vagrant(没用过,据说好用)<br>反编译jar:jd-gui<br>局域网会议软件:InletexEMC<br>excel插件:PyXLL(能直接执行python脚本)</p>
</blockquote>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>开发工具技巧</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式笔记</title>
    <url>/article/37573.html</url>
    <content><![CDATA[<h1 id="正则的用途"><a href="#正则的用途" class="headerlink" title="正则的用途"></a>正则的用途</h1><p>在很多地方都会用到正则表达式,如果熟练掌握正则,在爬虫,一些渗透实验中会占有优势</p>
<h1 id="正则的发展历程"><a href="#正则的发展历程" class="headerlink" title="正则的发展历程"></a>正则的发展历程</h1><p>看了一些总结正则发展历程的文章,我想大概总结下正则的几种标准:</p>
<p>从一开始的grep,再到egrep( 这里的e表示extended，加强版的意思,支持了+、|与?这三个元符号，并且可以在分组中使用*，分组可以直接写成(…)，同时用\1,\2…来引用分组。 )诞生,再到一系列类似sed,awk类的程序产生,这是时候是正则发展的混乱期,因为始终都没有一个标准.</p>
<blockquote>
<p>这种混乱度情况一直持续到了1986年。在1986年，POSIX（Portable Operating System Interface）标准公诸于世，POSIX制定了不同的操作系统都需要遵守的一套规则，当然，正则表达式也包括其中。<br>当然，除了POSIX标准外，还有一个Perl分支，也就是我们现在熟知的PCRE，随着Perl语言的发展，Perl语言中的正则表达式功能越来越强悍，为了把Perl语言中正则的功能移植到其他语言中，PCRE就诞生了。现在的编程语言中的正则表达式，大部分都属于PCRE这个分支。</p>
</blockquote>
<h1 id="正则标准分类"><a href="#正则标准分类" class="headerlink" title="正则标准分类"></a>正则标准分类</h1><h2 id="POSIX标准"><a href="#POSIX标准" class="headerlink" title="POSIX标准"></a>POSIX标准</h2><p>POSIX把正则表达式分为两种（favor）：BRE（Basic Regular Expressions）与ERE（Extended Regular Expressions ）。所有的POSIX程序可以选择支持其中的一种。具体对比引用别人的图,如下表：<br><img src="/uploads/2017-09-28_10-21-40.png" alt="BRE和ERE的区别"></p>
<p>翻译过来就是:</p>
<table>
<thead>
<tr>
<th>正则功能</th>
<th>BREs</th>
<th>EREs</th>
</tr>
</thead>
<tbody><tr>
<td><code>.,^,$,[...],[!...]</code>这四种用法</td>
<td>支持</td>
<td>支持</td>
</tr>
<tr>
<td>用<code>*</code>代表任意数量的字符匹配</td>
<td>支持</td>
<td>支持</td>
</tr>
<tr>
<td>用<code>+,?</code>分别代表一个以上和任意多个的字符匹配</td>
<td>不支持</td>
<td>支持</td>
</tr>
<tr>
<td>范围确定</td>
<td>写法例子为\{1,10\}</td>
<td>写法例子为{1,10}</td>
</tr>
<tr>
<td>分组</td>
<td>写法例子为\(…\)</td>
<td>写法例子为(…)</td>
</tr>
<tr>
<td>在括号分组后面指定分组出现次数</td>
<td>支持</td>
<td>支持</td>
</tr>
<tr>
<td>反向引用</td>
<td>写法例子为<code>\1</code></td>
<td>不支持</td>
</tr>
<tr>
<td>用竖线表示多种情况匹配</td>
<td>不支持</td>
<td>支持</td>
</tr>
</tbody></table>
<p>我们现在使用的linux发行版，都是集成GNU套件的，GNU是Gnu’s Not Unix的缩写，GNU在实现了POXIS标准的同时，做了一定的扩展，所以上面空白栏中的功能也能使用。下面一一讲解：</p>
<ol>
<li>BRE如何使用<code>+</code>、<code>?</code>呢？需要用<code>\+</code>、<code>\?</code></li>
<li>BRE如何使用<code>|</code>呢？需要用<code>\|</code></li>
<li>ERE如何使用<code>\1</code>、<code>\2</code>等这样的反引用？和BRE一样，就是<code>\1</code>、<code>\2</code>等等</li>
</ol>
<p>－ **(默认)**使用BRE语法的命令有：grep、ed、sed、vim<br>－ **(默认)**使用ERE语法的命令有：egrep、awk、emacs</p>
<p>注意上面说的是默认情况下,比如 <code>sed</code> 通过 <code>-r</code> 选项就可以使用ERE了，大家到时自己man一下就可以了。</p>
<p>POSIX还定义了一些shorthand，具体如下：</p>
<ul>
<li>[:alnum:]</li>
<li>[:alpha:]</li>
<li>[:cntrl:]</li>
<li>[:digit:]</li>
<li>[:graph:]</li>
<li>[:lower:]</li>
<li>[:print:]</li>
<li>[:punct:]</li>
<li>[:space:]</li>
<li>[:upper:]</li>
<li>[:xdigit:]</li>
</ul>
<p>在使用这些shorthand时有一个约束：必须在[]中使用，也就是说如果像匹配0-9的数字，需要这么写[[:alnum:]]，取反就是[^[:alnum:]]。shorhand 在BRE与EBE中的用法相同。</p>
<p>如果用多了sed或者awk会发现,\b,\w,\d这些用不了,因为POSIX规范根本没有定义这些shorthand,这些是在PCRE中定义的。</p>
<h2 id="PCRE标准"><a href="#PCRE标准" class="headerlink" title="PCRE标准"></a>PCRE标准</h2><p>前面说到,以前随着Perl的不断发展，其支持的正则表达式的功能也越来越强大。Perl比较显诸特征之一是与sed与awk兼容，这造就了Perl成为第一个通用性脚本语言。</p>
<p>Perl语言应用是如此广泛，以至于其他语言开始移植Perl，最终Perl compatible（兼容）的PCRE诞生了，这其中包括了Tcl, Python, Microsoft’s .NET , Ruby, PHP, C&#x2F;C++, Java等等。</p>
<p>shorthand在POSIX与PCRE是不同的，PCRE中我们常用的有如下这些：</p>
<ul>
<li><code>\w</code> 表示<code>[a-zA-Z]</code></li>
<li><code>\W</code> 表示<code>[^a-zA-Z]</code></li>
<li><code>\s</code> 表示<code>[\t\r\n\f]</code></li>
<li><code>\S</code> 表示<code>[^\t\r\n\f]</code></li>
<li><code>\d</code> 表示<code>[1-9]</code></li>
<li><code>\D</code> 表示<code>[^1-9]</code></li>
<li><code>\&lt;</code> 表示一个单词的起始</li>
<li><code>\&gt;</code> 表示一个单词的结尾</li>
<li><code>\b</code> 代表着单词的开头或结尾,也就是单词的分界处,它只匹配一个位置</li>
</ul>
<h1 id="正则使用时候的注意点"><a href="#正则使用时候的注意点" class="headerlink" title="正则使用时候的注意点"></a>正则使用时候的注意点</h1><h2 id="关于哪个工具用的哪个正则标准"><a href="#关于哪个工具用的哪个正则标准" class="headerlink" title="关于哪个工具用的哪个正则标准"></a>关于哪个工具用的哪个正则标准</h2><p>记住一点,在某个语言中,像 Java、Python等,这些语言的正则表达式都是基于PCRE标准的;而Linux下使用各种处理文本的命令，是继承自POSIX标准，不过是由GNU扩展后的而已。<br><strong>注意,因为平时在编程语言里面用正则的多些,所以下面的注意点都是以PCRE标准说的.等下不要说我怎么在awk中测试不通过就尴尬了^_^.</strong></p>
<h2 id="正则的匹配方式"><a href="#正则的匹配方式" class="headerlink" title="正则的匹配方式"></a>正则的匹配方式</h2><p>正则是从左到右,依次匹配,如果匹配完了,作为第一个匹配结果,这个时候如果还没到字符串末尾,则再继续往后匹配,如果还能再匹配上,作为第二个匹配结果,直到遍历完该字符串,匹配结束,然后匹配结果会按照括号进行分组,分组从1下标开始,如果是0下标表示整个匹配到的字符串,下面有一个例子</p>
<p><img src="/uploads/2017-09-28_11-48-39.png" alt="匹配例子"></p>
<h2 id="正则的处理选项"><a href="#正则的处理选项" class="headerlink" title="正则的处理选项"></a>正则的处理选项</h2><p><strong>单行模式和多行模式要特别注意下</strong></p>
<table>
<thead>
<tr>
<th>名称</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>IgnoreCase(忽略大小写)</td>
<td>匹配时不区分大小写。</td>
</tr>
<tr>
<td>Multiline(多行模式)</td>
<td>更改^和$的含义，使它们分别在任意一行的行首和行尾匹配，而不仅仅在整个字符串的开头和结尾匹配。(<strong>在此模式下,$的精确含意是:匹配\n之前的位置以及字符串结束前的位置.</strong>)</td>
</tr>
<tr>
<td>Singleline(单行模式)</td>
<td>更改.的含义，使它与每一个字符匹配（<strong>包括换行符\n</strong>）。</td>
</tr>
<tr>
<td>IgnorePatternWhitespace(忽略空白)</td>
<td>忽略表达式中的非转义空白并启用由#标记的注释。</td>
</tr>
<tr>
<td>ExplicitCapture(显式捕获)</td>
<td>仅捕获已被显式命名的组。</td>
</tr>
</tbody></table>
<h2 id="贪婪与懒惰"><a href="#贪婪与懒惰" class="headerlink" title="贪婪与懒惰"></a>贪婪与懒惰</h2><p><code>I already explained that the regex engine is eager .</code></p>
<p>这话的意思很明显,说明正则默认情况下,都是很贪婪的,比如用<code>get|gettest</code>去匹配<code>gettest</code>,一匹配到get后,就认为已经匹配成功了该交替表达式,结束匹配,返回get.<br>再举个例子,比如用<code>\b(Get(Value)?|Set(Value)?)\b</code>去匹配<code>SetValue</code>,因为使用了交替表达式,到了<code>Set(Value)?</code>这步匹配,因为贪婪,所以尽量长的去匹配,所以最终匹配到的是<code>SetValue</code>.</p>
<p>当正则表达式中包含能接受重复的限定符时，通常的行为是（在使整个表达式能得到匹配的前提下）匹配尽可能多的字符。以这个表达式为例<code>a.*b</code>，它将会匹配最长的以a开始，以b结束的字符串。如果用它来搜索<code>aabab</code>的话，它会匹配整个字符串<code>aabab</code>。这被称为贪婪匹配。</p>
<p>有时，我们更需要懒惰匹配，也就是匹配尽可能少的字符。限定符可以把贪婪模式转化为懒惰匹配模式，只要在它后面加上一个问号?。这样.*?就意味着匹配任意数量的重复，但是在能使整个匹配成功的前提下使用最少的重复。现在看看懒惰版的例子吧：</p>
<p><code>a.*?b</code>匹配最短的，以a开始，以b结束的字符串。如果把它应用于<code>aabab</code>的话，它会匹配aab（第一到第三个字符）和ab（第四到第五个字符）。</p>
<p>懒惰限定符:</p>
<table>
<thead>
<tr>
<th>代码&#x2F;语法</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>*?</td>
<td>重复任意次，但尽可能少重复</td>
</tr>
<tr>
<td>+?</td>
<td>重复1次或更多次，但尽可能少重复</td>
</tr>
<tr>
<td>??</td>
<td>重复0次或1次，但尽可能少重复</td>
</tr>
<tr>
<td>{n,m}?</td>
<td>重复n到m次，但尽可能少重复</td>
</tr>
<tr>
<td>{n,}?</td>
<td>重复n次以上，但尽可能少重复</td>
</tr>
</tbody></table>
<h2 id="时刻记得转义的存在"><a href="#时刻记得转义的存在" class="headerlink" title="时刻记得转义的存在"></a>时刻记得转义的存在</h2><p>比如你要查找<code>*</code>字符,这个字符在正则里面有特殊的意义,所以你得写成<code>\*</code>才能真正匹配到<code>*</code>,就跟程序里面写字符串要进行转义一样.这里还有个注意点,比如你想找<code>|</code>,正则就得写成<code>\|</code>,但是到了程序里面,又得多一层转义用来转义<code>\</code>这个字符,所以程序里面得写成 <code>String regex=&quot;\\|&quot;</code></p>
<h2 id="字符类的否定（negation）"><a href="#字符类的否定（negation）" class="headerlink" title="字符类的否定（negation）"></a>字符类的否定（negation）</h2><p>你可以通过在最开始的位置使用插入符号（译者注：^）来_否定_一个字符类。</p>
<p>[^a]表示“匹配除了a的任意字符”。<br>[^a-zA-Z0-9]表示“找到一个非字母数字字符”。<br>[^abc]表示“找到一个插入符或者a或者b或者c”。<br>[^^]表示“找到除了插入符外的任意字符”。（呕！）</p>
<h2 id="分组的使用"><a href="#分组的使用" class="headerlink" title="分组的使用"></a>分组的使用</h2><p>分组的使用这里我遇到几种情况:</p>
<ol>
<li>分组命名</li>
<li>忽略分组</li>
<li>嵌套分组</li>
<li>反向引用分组</li>
</ol>
<p>关于嵌套分组的有总结得比较好的,就不解释了:<br><img src="/uploads/2017-09-28_15-50-57.png" alt="嵌套分组"></p>
<p>剩下的直接上例子最好说明了:<br><img src="/uploads/2017-09-28_11-24-11.png" alt="使用分组"><br><img src="/uploads/2017-09-28_15-30-36.png" alt="使用分组命名并引用相应命名的分组"><br><img src="/uploads/2017-09-28_15-35-36.png" alt="忽略分组"><br><img src="/uploads/2017-09-28_11-24-57.png" alt="使用分组并反向引用分组"></p>
<h2 id="零宽断言和负向零宽断言"><a href="#零宽断言和负向零宽断言" class="headerlink" title="零宽断言和负向零宽断言"></a>零宽断言和负向零宽断言</h2><h3 id="零宽断言"><a href="#零宽断言" class="headerlink" title="零宽断言"></a>零宽断言</h3><p>通俗点讲,就是限制需要匹配的字符串前后能有什么,不能有什么.</p>
<p><code>(?=exp)</code>也叫零宽度正预测先行断言，它断言自身出现的位置的后面能匹配表达式exp。比如<code>\b\w+(?=ing\b)</code>，匹配以ing结尾的单词的前面部分(除了ing以外的部分)，如查找<code>I&#39;m singing while you&#39;re dancing.</code>时，它会匹配<code>sing</code>和<code>danc</code>。</p>
<p><code>(?&lt;=exp)</code>也叫零宽度正回顾后发断言，它断言自身出现的位置的前面能匹配表达式exp。比如<code>(?&lt;=\bre)\w+\b</code>会匹配以re开头的单词的后半部分(除了re以外的部分)，例如在查找<code>reading a book</code>时，它匹配<code>ading</code>。</p>
<p>下面这个例子同时使用了这两种断言<code>(?&lt;=\s)\d+(?=\s)</code>匹配以空白符间隔的数字(再次强调，不包括这些空白符)。</p>
<h3 id="负向零宽断言"><a href="#负向零宽断言" class="headerlink" title="负向零宽断言"></a>负向零宽断言</h3><p>那有了这个<strong>零宽断言</strong>,为什么还要有另外一个<strong>负向零宽断言</strong>呢?</p>
<p>看下下面的例子(引用,因为这个例子已经足够好说明了):</p>
<p><img src="/uploads/2017-09-28_15-12-03.png" alt="匹配包含p但是后面不要带有u的单词"></p>
<p><code>(?!exp)</code>，断言此位置的后面不能匹配表达式exp。它只匹配一个位置，并不消费任何字符。<br><code>(?&lt;!exp)</code>，断言此位置的前面不能匹配表达式exp。它只匹配一个位置，并不消费任何字符。</p>
<h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>总算写完了这篇文章,参考了许多资料,一开始接触awk,sed程序发现,跟java里面用的正则又不太一样,才有了后面去探索为什么有这些区别,顺便翻了下正则的发展史,总的来说正则的使用思路才清晰了些.这里只是把一些常用的用法在这里做了下汇总和总结,有错误的地方欢迎指正.</p>
<p>在java中,用<code>Pattern.compile(String inputString)</code>来产生一个<code>Pattern</code>类,<code>Pattern</code>类通过<code>Pattern.matcher(String regexString)</code>产生一个<code>Matcher</code>类,该<code>Matcher</code>类有group(用来获取分组)方法,start 和 end 方法,matches 和 lookingAt 方法,replaceFirst 和 replaceAll 方法,appendReplacement 和 appendTail 方法,具体使用详情请看: <a href="http://www.runoob.com/java/java-regular-expressions.html">菜鸟教程</a> ;快速测试java代码,可以在线使用 <a href="http://www.javarepl.com/term.html">Java Repl</a> 检验下.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在js中,正则表达式中/i,/g,/ig,/gi,/m的区别和含义</span><br><span class="line">/i (忽略大小写)</span><br><span class="line">/g (全文查找出现的所有匹配字符)</span><br><span class="line">/m (多行查找)</span><br><span class="line">/gi(全文查找、忽略大小写)</span><br><span class="line">/ig(全文查找、忽略大小写)</span><br><span class="line">参考文献:http://deerchao.net/tutorials/regex/regex.htm</span><br></pre></td></tr></table></figure>

<p>而有关正则的原理,则涉及到状态机,回溯那些东西,有机会再写下自己的理解^_^</p>
<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><p><img src="https://www.regular-expressions.info/tutorial.html" alt="正则表达式的详细说明(英文版)"><br><img src="http://liujiacai.net/blog/2014/12/07/regexp-favors/" alt="正则标准详细说明1"><br><img src="http://www.infoq.com/cn/news/2011/07/regular-expressions-6-POSIX" alt="正则标准详细说明2"><br><img src="http://www.cnblogs.com/deerchao/archive/2006/08/24/zhengzhe30fengzhongjiaocheng.html" alt="PCRE标准"><br><img src="https://segmentfault.com/a/1190000004429477" alt="关于正则分组的使用(有程序例子)"><br><img src="http://coolshell.cn/articles/9104.html" alt="sed的教程"><br><img src="https://zhuanlan.zhihu.com/p/27417442" alt="正则表达式回溯法原理"></p>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>正则表达式</tag>
      </tags>
  </entry>
  <entry>
    <title>语言与规则处理工具</title>
    <url>/article/36229.html</url>
    <content><![CDATA[<h2 id="解析器生成器（Parser-Generator）"><a href="#解析器生成器（Parser-Generator）" class="headerlink" title="解析器生成器（Parser Generator）"></a>解析器生成器（Parser Generator）</h2><h3 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h3><p>解析器生成器是一种<strong>工具</strong>，它根据用户定义的语法规则（通常是上下文无关文法），自动生成能够将<strong>字符串</strong>转换为<strong>抽象语法树（AST）</strong> 的代码。它解决的是“如何将文本解析为计算机可理解的结构”这一底层问题。</p>
<h3 id="典型框架"><a href="#典型框架" class="headerlink" title="典型框架"></a>典型框架</h3><ul>
<li><strong>ANTLR</strong>（支持 Java、Python、JavaScript 等多种语言）</li>
<li><strong>JavaCC</strong>（Java）</li>
<li><strong>Yacc &#x2F; Bison</strong>（C&#x2F;C++）</li>
<li><strong>PEG.js</strong>（JavaScript）</li>
<li><strong>Lark</strong>（Python）</li>
</ul>
<h3 id="应用领域"><a href="#应用领域" class="headerlink" title="应用领域"></a>应用领域</h3><ul>
<li><strong>开发编程语言</strong>（如 Kotlin、Groovy 早期版本使用 ANTLR）</li>
<li><strong>设计领域特定语言（DSL）</strong>（如 SQL 解析、配置文件语法）</li>
<li><strong>协议解析</strong>（如 JSON、XML、自定义二进制协议）</li>
<li><strong>静态分析工具</strong>（如代码格式化器、lint 工具）</li>
</ul>
<hr>
<h2 id="表达式解析引擎（Expression-Evaluation-Engine）"><a href="#表达式解析引擎（Expression-Evaluation-Engine）" class="headerlink" title="表达式解析引擎（Expression Evaluation Engine）"></a>表达式解析引擎（Expression Evaluation Engine）</h2><h3 id="定义-1"><a href="#定义-1" class="headerlink" title="定义"></a>定义</h3><p>表达式解析引擎专门用于<strong>计算单个表达式</strong>（如 <code>a + b * c &gt; d</code>）的值。它内部通常也包含解析器（自己实现或用解析器生成器），但对外提供的是简单、直接的求值接口。这类引擎不关心复杂的规则组合和推理。</p>
<h3 id="典型框架-1"><a href="#典型框架-1" class="headerlink" title="典型框架"></a>典型框架</h3><ul>
<li><strong>Aviator</strong>（Java，高性能，语法接近 Java）</li>
<li><strong>MVEL</strong>（Java，支持表达式和简单脚本）</li>
<li><strong>Spring Expression Language (SpEL)</strong>（Java，与 Spring 生态集成）</li>
<li><strong>ExprTK</strong>（C++）</li>
<li><strong>eval()</strong>（多种语言内置，但安全性较差）</li>
</ul>
<h3 id="应用领域-1"><a href="#应用领域-1" class="headerlink" title="应用领域"></a>应用领域</h3><ul>
<li><strong>动态公式计算</strong>（如 Excel 公式、保险费率计算）</li>
<li><strong>配置条件判断</strong>（如功能开关：<code>${user.isVip &amp;&amp; order.amount &gt; 100}</code>）</li>
<li><strong>报表系统</strong>（用户自定义计算字段）</li>
<li><strong>权限表达式</strong>（如 <code>subject.hasRole(&#39;admin&#39;)</code>）</li>
</ul>
<hr>
<h2 id="规则引擎（Rule-Engine）"><a href="#规则引擎（Rule-Engine）" class="headerlink" title="规则引擎（Rule Engine）"></a>规则引擎（Rule Engine）</h2><h3 id="定义-2"><a href="#定义-2" class="headerlink" title="定义"></a>定义</h3><p>规则引擎用于管理、执行一组<strong>业务规则</strong>，支持基于事实（Fact）的<strong>模式匹配</strong>、推理链、冲突解决等。它处理的是“如何根据多条规则做出复杂决策”的问题，而非单个表达式计算。</p>
<h3 id="典型框架-2"><a href="#典型框架-2" class="headerlink" title="典型框架"></a>典型框架</h3><ul>
<li><strong>Drools</strong>（Java，功能最全面的开源规则引擎，支持 Rete 算法）</li>
<li><strong>EasyRules</strong>（Java，轻量级）</li>
<li><strong>Jess</strong>（Java，基于 CLIPS）</li>
<li><strong>ILOG JRules &#x2F; IBM ODM</strong>（商业）</li>
<li><strong>NRules</strong>（.NET）</li>
</ul>
<h3 id="应用领域-2"><a href="#应用领域-2" class="headerlink" title="应用领域"></a>应用领域</h3><ul>
<li><strong>风控系统</strong>（如贷款审批、反欺诈）</li>
<li><strong>业务规则管理（BRMS）</strong>（如促销规则、折扣计算）</li>
<li><strong>工作流&#x2F;审批流</strong>（规则驱动的路由）</li>
<li><strong>实时决策</strong>（如物联网设备规则引擎）</li>
</ul>
<hr>
<h2 id="对比"><a href="#对比" class="headerlink" title="对比"></a>对比</h2><table>
<thead>
<tr>
<th>维度</th>
<th>解析器生成器</th>
<th>表达式解析引擎</th>
<th>规则引擎</th>
</tr>
</thead>
<tbody><tr>
<td><strong>核心任务</strong></td>
<td>将字符串转换为语法树</td>
<td>计算单个表达式值</td>
<td>根据一组规则对事实进行推理决策</td>
</tr>
<tr>
<td><strong>输入</strong></td>
<td>语法定义文件（如 <code>.g4</code>）</td>
<td>表达式字符串 + 变量上下文</td>
<td>规则集 + 事实对象</td>
</tr>
<tr>
<td><strong>输出</strong></td>
<td>词法&#x2F;语法分析器代码</td>
<td>计算结果（如布尔值、数值）</td>
<td>执行结果（如事实更新、动作触发）</td>
</tr>
<tr>
<td><strong>复杂度</strong></td>
<td>高（需要编写语法）</td>
<td>低（开箱即用）</td>
<td>高（需理解规则语言、推理算法）</td>
</tr>
<tr>
<td><strong>典型框架</strong></td>
<td>ANTLR, JavaCC, Yacc</td>
<td>Aviator, MVEL, SpEL</td>
<td>Drools, EasyRules, Jess</td>
</tr>
<tr>
<td><strong>应用层次</strong></td>
<td><strong>基础设施层</strong>（构建工具）</td>
<td><strong>应用层</strong>（轻量计算）</td>
<td><strong>业务层</strong>（复杂决策）</td>
</tr>
<tr>
<td><strong>是否包含解析器</strong></td>
<td>自身就是生成解析器的工具</td>
<td>内部自带简易解析器</td>
<td>通常自带规则语言解析器（可能基于解析器生成器实现）</td>
</tr>
</tbody></table>
<hr>
<h2 id="三者关系"><a href="#三者关系" class="headerlink" title="三者关系"></a>三者关系</h2><ul>
<li><strong>解析器生成器</strong>是<strong>构建工具</strong>，用于生成解析器，它本身不直接提供表达式求值或规则执行。但表达式引擎和规则引擎都可以基于它来定义自己的语言（例如 Drools 的 DRL 语法早期曾使用 ANTLR 解析）。</li>
<li><strong>表达式解析引擎</strong>可以看作是<strong>规则引擎的一个子组件</strong>——规则的条件部分往往需要表达式求值。因此一些规则引擎内部会集成一个表达式引擎。</li>
<li><strong>规则引擎</strong>是<strong>最上层的业务决策系统</strong>，它可能需要解析器生成器来解析规则语言，也需要表达式引擎来评估条件，但它提供的功能远远超越单纯的解析和求值。</li>
</ul>
<hr>
<h2 id="如何选择"><a href="#如何选择" class="headerlink" title="如何选择"></a>如何选择</h2><ul>
<li>如果你需要<strong>自定义语法、开发新语言或解析复杂文本格式</strong> → 选择 <strong>解析器生成器</strong>（如 ANTLR）。</li>
<li>如果你只需要<strong>动态计算公式或简单条件</strong> → 选择 <strong>表达式解析引擎</strong>（如 Aviator）。</li>
<li>如果你需要<strong>管理大量业务规则、支持规则的热部署、冲突解决</strong> → 选择 <strong>规则引擎</strong>（如 Drools）。</li>
</ul>
<p>三者并不互斥，在大型系统中常常联合使用：用解析器生成器定义规则 DSL，用表达式引擎计算条件，最后用规则引擎执行完整的规则集。</p>
<script type="text&#x2F;javascript" src="https://unpkg.com/kity@2.0.4/dist/kity.min.js"></script><script type="text&#x2F;javascript" src="https://unpkg.com/kityminder-core@1.4.50/dist/kityminder.core.min.js"></script><script defer="true" type="text&#x2F;javascript" src="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.js"></script><link rel="stylesheet" type="text&#x2F;css" href="https://unpkg.com/hexo-simple-mindmap@0.8.0/dist/mindmap.min.css">]]></content>
      <tags>
        <tag>语言与规则处理工具</tag>
      </tags>
  </entry>
</search>
